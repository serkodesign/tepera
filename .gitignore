# Android Studio / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Keystore / підписання застосунку — НІКОЛИ не комітити (SRS PUB-6). Уся папка /keystore/
# ігнорується цілком (не лише *.jks) — вона містить і текстовий файл з паролями, який жодним
# розширенням не покривається.
*.jks
*.keystore
key.properties
/keystore/

# Firebase (google-services.json містить проєктні ключі — не публічний секрет,
# але краще виключити з публічного репозиторію; підключити у фазі 5)
google-services.json
