# Baseline profile застосунку: код Tepera (екрани, ViewModel, репозиторії) компілюється наперед (AOT) при
# встановленні, а не інтерпретується/JIT-ується при першому запуску. Без цього перший перехід на кожен
# екран рахував композицію на холодному коді — ривок 100-200 мс у першому кадрі переходу.
# Профілі самих бібліотек (Compose, Room тощо) додаються автоматично з їхніх AAR.
# Широкі шаблони (**), бо застосунок невеликий — дрібна вибірка тут дала б менше користі, ніж вартує підтримка.
HSPLcom/serkodesign/tepera/**;->**(**)**
Lcom/serkodesign/tepera/**;
