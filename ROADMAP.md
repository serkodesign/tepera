# ROADMAP — Tepera (v2.5)

Розбивка фаз із SRS v2.0 (розділ 7) на конкретні задачі, Фази 0-6 нижче ще написані до
переосмислення ядра в SRS v2.2-v2.5 (лишені як історія — самі задачі виконані, лише термінологія
застаріла: "Online/Offline баланс" → "Структура доби", "Сон" був серед дефолтних категорій).
Актуальний стан ядра — розділ "SRS v2.5 rebase" нижче. Працювати по одній фазі за раз.
Позначай `[x]` по завершенню.

## Фаза 0 — Налаштування та архітектура (~2 тижні)
- [x] Вирішено: applicationId, категорії, віджет без live-таймера, Health Connect → post-MVP
- [x] Створити Gradle-проєкт з наявного скелету (`./gradlew :app:assembleDebug` збирається)
- [x] Підключити Room, Compose, Vico, Glance, WorkManager (Crashlytics — фаза 5)
- [x] Перенести Kotlin-файли моделі даних (entity, dao, repository)
- [x] AppDatabase з явною міграційною стратегією (schema export у `app/schemas/`, без `fallbackToDestructiveMigration()`)
- [x] Repository-абстракція: Category, ActivityEntry, ExcludedApp, Balance
- [x] Базовий Theme.kt (заглушка до Figma)
- [x] Локальний анонімний device-ID (DataStore) — `DeviceIdProvider`
- [x] Git-репозиторій з `.gitignore` для keystore (відновлено після випадкового видалення)
- [ ] Google Play Developer акаунт — паралельна задача, не автоматизується звідси

## Фаза 1 — Базові екрани (~4 тижні)
- [x] **Підтвердити 5 дефолтних категорій зі стейкхолдером перед стартом** — вже підтверджено в SRS розділі 11 (Природа, Читання, Хобі/Творчість, Рух/Спорт, Сон)
- [x] Category CRUD (5 дефолтних + 1 користувацька, архівація — не видалення) — перевірено на Samsung S23: архівація переносить у розділ "Архівовано", створення кастомної категорії з іконкою/кольором зі списку
- [x] Екран Home/Сьогодні — реактивний підсумок хвилин по категоріях за сьогодні
- [x] Екран додавання запису (пресети/ручний ввід/інтервал, нотатка ≤250 символів) — перевірено вживу, збереження переживає перезапуск застосунку
- [x] Перевірка перекриття часу — лише в межах однієї категорії (діалог "зберегти все одно" підключено до вже наявної DAO-логіки з Фази 0; логіку код-рев'южено, клік-тест не проводився)
- [x] **Розширення Home (поза первинним обсягом фази, додано за запитом):** розархівація
      категорій з UI (`CategoryRepository.unarchive()` — раніше архівація була дорогою в один
      кінець); тап по картці категорії починає живий таймер (`ActiveTimerStore`, DataStore —
      переживає смерть процесу), повторний тап зупиняє й зберігає запис, кілька категорій
      можуть таймитись одночасно; картки категорій — сітка 3x2 (`LazyVerticalGrid`) замість
      вертикального списку. Перевірено вживу на Samsung S23
- [x] **Багфікс (за запитом):** зупинка тап-таймера рахувала хвилину округленням (`roundToInt`)
      і примусовим мінімумом 1 хв — тап на 2 сек все одно логував повну хвилину. Замінено на
      floor від секунд без примусового мінімуму: запис коротший за хвилину не створюється
      взагалі. Живий лічильник (`M:SS`) і статичний підпис хвилин прибрано з карток категорій
      (за запитом) — лишились лише іконка й кольорова рамка як індикатор активного таймера.
- [x] **Навігація — нижній навбар (за запитом):** 3 вкладки (Home/Статистика/Меню) замість трьох
      окремих іконок у TopAppBar Home; Категорії переїхали з іконки на Home всередину Меню
      (=Налаштування), поруч з Exclusion List. Кнопка "Додати активність" — кругла, лише іконка
      плюс, без тексту.

## Фаза 2 — Online/Offline баланс (~3 тижні)
*(нова фаза замість Health Connect/Sleep API з v1.5)*
- [x] **Спайк:** перевірено на реальному пристрої (Samsung S23, Android 16). `queryUsageStats()`
      справді повертає імена пакетів незалежно від package visibility filtering (окремий
      AppOps-грант, не PackageManager query), але резолвити людську назву/іконку через
      `getApplicationInfo()` без додаткової видимості НЕ вдалося — 226 із 580 пакетів, побачених
      за 30 днів, кидали `NameNotFoundException`, включно з реальним застосунком (Pocket Casts).
      Друга проблема: без фільтрації "580 пакетів" — це здебільшого системний шум
      (drivers/RRO-оверлеї), не реальні застосунки. **Рішення без QUERY_ALL_PACKAGES:**
      `<queries>` з intent-фільтром MAIN/LAUNCHER у маніфесті — не вважається чутливим дозволом
      при Google Play review (на відміну від QUERY_ALL_PACKAGES), відсікає системний шум і
      повертає видимість усіх launchable застосунків. Після фіксу: 113 релевантних застосунків,
      0 unresolved. Exclusion List будується саме так.
- [x] Онбординг-екран з поясненням, навіщо потрібен доступ до статистики використання —
      показується автоматично один раз (DataStore-прапорець), перевірено вживу
- [x] Інтеграція UsageStatsManager, читання Online-часу за добу — перевірено вживу з реальними
      даними після надання доступу
- [x] Grace Period Buffer формула, скидання о 00:00 — перенесено з Фази 0 (BalanceRepository),
      маркер таргету на шкалі відображається коректно
- [x] Налаштування таргету (дефолт 180 хв) — SettingsStore (DataStore), екран Налаштувань
- [x] Exclusion List — вибір застосунків зі списку встановлених — перевірено вживу (Карти,
      увімкнення/вимкнення перемикача переживає перезапуск)
- [x] Fallback UI без наданого дозволу — картка балансу на Home показує заклик до дії, поки
      доступ не надано; офлайн-логування (Фаза 1) увесь час лишається робочим
- [x] **Крашфікс:** `queryUsageStats()` кидає `SecurityException` (не повертає порожній список,
      як припускав коментар у коді), якщо доступ був наданий, а тоді вимкнений вручну в
      Налаштуваннях — застосунок падав замість показу fallback. Відтворено на реальному
      пристрої через `adb shell appops set ... android:get_usage_stats deny`; `hasUsageAccess()`
      і `getOnlineMinutes()` у `BalanceRepository` тепер ловлять `SecurityException`
- [x] **UI-покращення (за запитом):** картка балансу — два окремі підписані пропорційні бари
      (Online/Offline зі своїми хвилинами), а не одна шкала без явного відображення Offline;
      маркер таргету лишився на Online-барі. Перевірено вживу
- [x] **Багфікс (за запитом):** `getOnlineMinutes()` через `queryUsageStats(INTERVAL_DAILY, ...)`
      завищував Online-час (159 хв у Tepera проти 47 хв у системному Samsung Digital Wellbeing за
      той самий момент) — межі бакетів статистики не обрізані рівно по `[from, to]`. Замінено на
      підрахунок по сирих подіях `UsageEvents` (MOVE_TO_FOREGROUND/MOVE_TO_BACKGROUND), обрізаних
      точно по запитаному інтервалу — той самий підхід, що й системні лічильники екранного часу.
- [x] **UI-покращення (за запитом):** картка балансу на Home перероблена під стиль віджета — один
      сумісний бар (Online-частка + Offline-частка на одній лінії) з відсотками над ним
      ("Online: 43% · Offline: 57%") замість двох окремих шкал.
- [x] **Багфікс (за запитом):** лаунчер і клавіатура виключені з підрахунку Online-хвилин за
      замовчуванням, поверх ручного Exclusion List — визначаються динамічно через
      `PackageManager`/`InputMethodManager`, не захардкоджені пакети. Перевірено вживу на
      Samsung S23: 67 хв → 59 хв за той самий момент після виключення.

## Фаза 3 — Віджет (~3 тижні)
- [x] Jetpack Glance: компактна 4x1 (до 5 кнопок категорій, hit-box ≥48dp, розмір кнопки
      адаптується до реальної ширини від launcher-а — targetCellWidth не гарантує однакову
      ширину в dp на всіх launcher-ах) — перевірено вживу (Microsoft Launcher)
- [x] Розширена 4x2 (+ шкала балансу з маркером таргету) — без Canvas (Glance/RemoteViews
      його не має), шкала імітується решіткою з 20 рівних сегментів; перевірено вживу
- [x] WorkManager — періодичне оновлення (~30 хв, `WidgetUpdateWorker`, `ExistingPeriodicWorkPolicy.KEEP`)
- [x] Quick Settings tile — код-рев'ю (той самий механізм deep-link, що й кнопки віджета,
      уже перевірений вживу), живий клік по tile не тестувався
- [x] Контекстне сортування кнопок за часом доби — робочі пороги ранок/день/вечір/ніч
      (SRS не фіксує точні межі), перевірено вживу для періоду "вечір"
- [x] "5-й слот" — підказка категорії, не логованої >3 днів — підсвічене кільце навколо
      кнопки, перевірено вживу
- [x] **Кнопки категорій на віджеті — тап-таймер (поза первинним обсягом фази, додано за
      запитом):** тап починає/зупиняє той самий таймер, що на Home (спільна
      `toggleCategoryTimer()`), `actionStartActivity` замінено на Glance `ActionCallback`.
      Активний стан — контрастне кільце + "■" замість літери. **Багфікс:** умовне
      застосування `.background()` (то є, то немає) лишало кільце "застряглим" після зупинки
      через баг RemoteViews-діфінгу в Glance — виправлено викликом `.background()` завжди,
      з прозорим кольором за замовчуванням. Перевірено вживу на Samsung S23 (Microsoft
      Launcher): старт/стоп коректні, запис зберігається з правильною тривалістю.

## Фаза 4 — Статистика та історія (~2 тижні)
- [x] Vico-графіки розподілу офлайн-часу по категоріях (День/Тиждень/Місяць) — новий екран
      "Статистика" (іконка на Home), стовпчикова діаграма, підписи категорій через
      CartesianValueFormatter + ExtraStore; Тиждень/Місяць — ковзне вікно 7/30 днів, не
      календарний період (спрощення MVP); перевірено вживу на Samsung S23
- [x] Тижневий тренд Online/Offline балансу — лінійний графік Online-ratio (Grace Period Buffer
      формула для сьогодні, повна доба 1440 хв для завершених днів) за останні 7 днів,
      fallback-картка без наданого доступу до статистики використання; перевірено вживу

## Фаза 5 — Локалізація, доступність, бекап (~2 тижні)
- [x] UA/EN у strings.xml — аудит усього UI виявив 2 хардкод-рядки поза ресурсами
      (TimeField у AddEntryScreen, "OK" у CategoriesScreen), обидва виправлені; повна
      відповідність ключів між values/ і values-en/ підтверджена
- [x] Android Auto Backup + ручний JSON експорт/імпорт — `backup_rules.xml`/
      `data_extraction_rules.xml` фіналізовані (повне включення, свідомо без винятків,
      SRS розділ 5.2); новий `BackupRepository` (org.json, без нової залежності) + дві дії в
      Налаштуваннях через SAF (`CreateDocument`/`OpenDocument`); імпорт ПОВНІСТЮ замінює
      локальні дані (категорії/записи/excluded apps/таргет) в одній Room-транзакції, з
      діалогом підтвердження; перевірено вживу — експорт → перегляд JSON adb → імпорт →
      дані на Home ідентичні початковим
- [x] Firebase Crashlytics — проєкт `tepera-8d00f` створено користувачем у Firebase Console,
      `google-services.json` покладено в `app/` (у .gitignore з Фази 0, не в репозиторії).
      Підключено окремий Gradle-плагін `com.google.firebase.crashlytics` (не лише google-services —
      з версії Crashlytics Gradle plugin 3.x потрібен для завантаження mapping-файлів),
      `firebase-bom:34.18.0` + `firebase-crashlytics` — СВІДОМО без `firebase-analytics`
      (CLAUDE.md: лише crash-репортинг, жодної usage-аналітики). Перевірено вживу: застосунок
      запускається без крашу, FirebaseSessions успішно фетчить remote-налаштування (мережеве
      з'єднання з Firebase підтверджено)
- [x] Базова accessibility (системні дефолти) — усі 5 кнопок "Назад" у TopAppBar мали
      `contentDescription = null` (не оголошувались для TalkBack); додано спільний рядок
      `nav_back` і застосовано на всіх екранах
- [x] **Перемикач мови застосунку (поза первинним обсягом фази, додано за запитом):**
      3-way перемикач Системна/Українська/English у Налаштуваннях.
- [x] **Багфікс (за запитом, "перемикач мови не працює"):** перша реалізація через
      `AppCompatDelegate.setApplicationLocales()` (`androidx.appcompat`) лише запам'ятовувала
      вибір — та бібліотека застосовує мову до ресурсів лише для `AppCompatActivity`, а
      `MainActivity` звичайний `ComponentActivity`, тож UI не перемикався (підтверджено на
      Samsung S23: "English" позначався, текст лишався українською). Замінено на
      `util/LocaleStore.kt` (SharedPreferences + ручне обгортання контексту через
      `MainActivity.attachBaseContext()`) + `Activity.recreate()` після зміни вибору; працює
      однаково на всіх API-рівнях (26+). `androidx.appcompat` прибрано із залежностей.
      Перевірено вживу на Samsung S23: UA→EN, EN→UA, і збереження після force-stop.

## Фаза 6 — Інтеграція дизайну з Figma (1-2 тижні, паралельно з фазами 1-5)
- [x] **Home screen (частково, за запитом):** дизайн перенесено з Figma-фрейму
      `Everyday_Designs` (файл `UyVsim0I8KSwt6qHakkdxp`, Home screen node 1930:233) через
      Figma MCP-конектор (`get_design_context`) на Home і Статистику — `TeperaPalette.kt`
      (кольори/градієнт з фрейму), привітання за часом доби, "Life balance" двома пропорційними
      блоками з позначкою таргету (`BalanceCard.kt` → `LifeBalanceSection`), картки категорій
      без кольорових кружків (тонована іконка + play/pause), нижній навбар-"таблетка" з "+"
      по центру (`TeperaNavHost.kt`). Налаштування/Категорії/Виключені застосунки/Додати
      активність — досі дефолтна Material 3 тема, для них Figma-фрейму ще нема.
      **Шрифти НЕ додані як залежність:** Lora (заголовки) замінена системним `FontFamily.Serif`
      (Google Fonts Downloadable API вимагає сертифікат Google Play Services — копіювати його
      наосліп з пам'яті занадто ризиковано), Satoshi (текст) — дефолтним sans (пропрієтарний
      шрифт, недоступний через Google Fonts).
      **Дизайн — ЗАВЖДИ light**, `TeperaTheme` більше не реагує на `isSystemInDarkTheme()`:
      системна темна тема вмикала `darkColorScheme()` з білим текстом, майже невидимим на
      світлому градієнті (відтворено й виправлено на Samsung S23). Додано
      `enableEdgeToEdge()` у `MainActivity` — інакше градієнт не сягав країв екрана
      (status bar/навбар лишались білою смугою поверх контенту).
      Перевірено вживу на Samsung S23: Home, Статистика, перехід у Налаштування й назад.
- [x] **Кнопка "Додати активність" — зелена (за запитом, той самий колір, що Offline-блок
      балансу), відступ нижнього навбару від краю екрана — 24dp (було 8dp).**
- [x] **Віджет — той самий стиль (за запитом):** літери-ініціали на кнопках категорій замінено
      реальними іконками (5 нових vector drawable `drawable/ic_widget_*` — RemoteViews не вміє
      відобразити androidx.compose.material `ImageVector` напряму, потрібен окремий спрощений
      набір; лише 5 дефолтних категорій, кастомна на віджеті не показується), тоновані
      категорійним кольором через Glance `ColorFilter.tint()`. Кнопки збільшено (стеля 48dp→56dp
      — FR-4.1 вимагає МІНІМУМ 48dp, стеля лише штучно обмежувала зростання), іконки
      центровано вертикально в 4x1 (`verticalAlignment = Alignment.CenterVertically` на
      зовнішньому Column). Зовнішній фон віджета — фіксований `TeperaPalette.navPill`
      (не `GlanceTheme.colors.background`, яке слідує системній темі) для узгодженості з
      "дизайн ЗАВЖДИ light". Перевірено вживу на Samsung S23.
- [x] **Налаштування/Категорії/Виключені застосунки/+ новий екран "Резервне копіювання"** —
      дизайн перенесено з тієї ж Figma-сторінки "Tepera" (node 1873:2567: фрейми "Settings",
      "Settings - Categories", "Setings - Excluded apps", "Settings - Backup and restore").
      Спільні "скляні" елементи — `ui/theme/TeperaComponents.kt` (`GlassScreenHeader` — кругла
      напівпрозора кнопка "назад" замість TopAppBar, `GlassRow`/`GlassSectionHeader` — картки-
      рядки, `PillSegmentedControl` — суцільна капсула для перемикача мови без ліній-розділювачів
      Material3 SegmentedButtonRow, `teperaSwitchColors()` — фірмовий темно-зелений `#005E3E`
      для Switch, той самий колір і для кружків-іконок навігаційних рядків). Резервне
      копіювання (Export/Import JSON) переїхало з інлайн-дій у Налаштуваннях на власний екран
      `BackupRestoreScreen.kt` — так у фреймі.
      **Категорії:** кнопка архів/розархівувати замінена на Switch (checked = активна) —
      функціонально та сама архівація/розархівація. У фреймі-мокапі нема кнопки "Додати свою
      категорію" (демо лише для 5 фіксованих), але це реальна вимога FR-2.2 — за узгодженням з
      користувачем додано як рядок унизу секції "Активні".
      **Виключені застосунки:** розділено на секції "Виключені"/"Усі" (було — плаский список).
      **За запитом користувача — реальні іконки застосунків, не порожні кружки-заглушки:**
      у мокапі демо-рядки типу "1000 hours"/"AGC Toolkit" показують ПОРОЖНІ кольорові кружки
      (Figma не мала реальних даних) — `InstalledAppsProvider` вже резолвить справжню іконку
      через `PackageManager` (з Фази 2), просто перевірено, що новий стиль її не губить;
      нейтральна заглушка лишається лише для рідкісного краю, де резолвінг справді не вдався.
      **Таргет Online-часу (ПЕРЕГЛЯНУТО, див. запис нижче):** спершу залишили довільні
      хвилини в капсулі-заглушці, пізніше за прямим запитом користувача замінено на
      справжній слайдер 1-8 год — деталі в наступному записі.
      Перевірено вживу на Samsung S23: усі 4 екрани, редагування таргету (з перезапуском
      застосунку), архівація/розархівація категорії через Switch.
- [x] **Таргет Online-часу — слайдер 1-8 год замість текстового поля/капсули-заглушки
      (за прямим запитом користувача, скасовує рішення "залишити хвилини" з попереднього
      запису):** `HourRangeSlider` (`ui/theme/TeperaComponents.kt`) — кастомний
      drag+tap-слайдер (`pointerInput`/`detectHorizontalDragGestures`+`detectTapGestures`),
      не Material3 `Slider` — дизайн вимагає суцільну заповнену капсулу на всю висоту, а
      не тонкий трек+повзунок. Частка заповнення = `hours / maxHours` (не
      `(hours-min)/(max-min)`) — підтверджено математикою Figma (чіп "3h" ≈ 37.5% ширини
      треку = 3/8). Значення округлюється до цілих годин і зберігається як хвилини
      (`hours * 60`) у той самий `SettingsStore.targetMinutes`, що й раніше — без міграції
      схеми. Перевірено вживу на Samsung S23: тап у будь-яку точку треку й перетягування
      обидва коректно змінюють значення (перевірено перехід 3 год → 7 год тапом).
- [x] **Home screen — картки категорій перебудовано, прибрано загальну кнопку "+" (за
      прямим запитом користувача, Figma node 1951:4017):** кожна картка категорії тепер має
      два елементи в нижньому ряду — широка "таблетка" play/pause (стара функція тап-таймера,
      без змін у логіці `HomeViewModel.toggleTimer`) і окрема кругла кнопка з іконкою
      `MoreTime` ("додати час"), яка одразу відкриває `AddEntryScreen` з ПОПЕРЕДНЬО ОБРАНОЮ
      цією категорією (новий параметр `HomeScreen.onAddEntryForCategory`, веде на
      `Routes.addEntry(categoryId)`). Загальної кнопки "+" на навбарі, яка раніше відкривала
      Add Entry БЕЗ обраної категорії, більше немає — цей вхідний сценарій повністю замінено
      на "додати час конкретній активності". Сітка категорій — 2 колонки (`GridCells.Fixed(2)`,
      було 3), картки вищі (132dp) й показують іконку+назву зверху. Блок "Баланс" тепер
      обгорнутий в одну напівпрозору картку (`cardTranslucentLight`) замість голого розміщення
      на фоні. Перевірено вживу на Samsung S23: вигляд сітки 2x2+2, тап на "додати час"
      відкриває Add Entry з правильною попередньо обраною категорією, тап на play/pause
      і далі pause коректно запускає/зупиняє таймер (картка стає білою, "таблетка" —
      темно-зеленою з іконкою pause).
- [x] **Нижній навбар перебудовано під новий дизайн (Figma node 1951:4017):** суцільна
      темно-зелена "таблетка" (`navPillDark`) замість напівпрозорої білої, 3 вкладки
      замість Home/Статистика/"+": Головна (обрана — іконка + підпис + підсвічений овал),
      Статистика (лише іконка), і третя вкладка-заглушка з іконкою `Pending` — за відповіддю
      користувача на уточнювальне питання ("Зроби її, але залиш поки неактивною") зроблена
      візуально (40% прозорості білого), але навмисно НЕФУНКЦІОНАЛЬНА (`enabled = false`,
      порожній `onClick`) до майбутнього рішення, що там буде. Перевірено вживу на
      Samsung S23: обрана вкладка Home підсвічена з підписом, тап на заглушку нічого не
      робить і не змінює екран.

## SRS v2.5 rebase — переосмислення ядра (розділи 3.3, 4 SRS)

Стейкхолдер оновив SRS з v2.1 до v2.5 (v2.2: "Online/Offline баланс" замінено трисегментною
структурою доби через доказову базу про сором/провину; v2.4: Сон прибрано з дефолтних категорій;
v2.5: окрему фазу "Інтеграція дизайну з Figma" прибрано з таймлайну — дизайн вважається вже
встановленим робочим стилем). Підхід узгоджено зі стейкхолдером: спершу ядро (Stage 1), потім
психологічні фічі (Stage 2) окремим заходом.

- [x] **Stage 1 — ядро:** `DefaultCategories` — Сон → Живе спілкування (`nameKey = "social"`,
      новий `ic_widget_social.xml`, іконка `Icons.Outlined.Groups`); `TeperaApp.onCreate()`
      архівує `default-sleep` ідемпотентно (`archive()`, не видалення). `SettingsStore.
      sleepWindowEndHour` (дефолт 6) — нове налаштування, свій `HourRangeSlider` у
      `SettingsScreen`. `BalanceRepository.calculateDayStartMillis()` — точка старту дня: перше
      суттєве розблокування (перший `MOVE_TO_FOREGROUND` після півночі), сесія до
      `sleepWindowEndHour` коротша за 5 хв ігнорується як нічна перевірка годинника (узгоджено зі
      стейкхолдером — SRS текст називав окремо поріг 2-3 хв і межу 05:00, об'єднано в один
      параметр). `calculateDenominatorMinutes()`/`getOnlineMinutesToday()` тепер від цієї точки,
      не від півночі. `BalanceViewModel`/`BalanceUiState` — три сегменти (Online + категорії з
      ненульовим часом сьогодні, кожна своїм кольором, + "Решта дня") замість двох Online/Offline
      блоків; `CategoryRepository` додано в `Factory` (для резолву кольору/назви категорій).
      `HomeScreen`/`BalanceCard.kt` → `MyDaySection`: заголовок "Твій день триває X" (FR-3.7,
      росте разом з реальним часом), багатосегментна шкала + легенда (кольоровий квадрат + назва
      + час, без голого %), тиха засічка орієнтиру БЕЗ підпису й БЕЗ зміни кольору при
      перевищенні (референс — Figma node 2002:170, файл `UyVsim0I8KSwt6qHakkdxp`, розділ 4.4
      SRS). Віджет (`TeperaWidget.kt`) — та сама тришарова шкала, 20-сегментна решітка (Glance
      без Canvas), заввишки 8dp (тихіша за кнопки), засічка орієнтиру нейтральним кольором
      (замість попереднього `GlanceTheme.colors.error`, який читався як тривога). Рядки:
      "Таргет"→"Орієнтир" (`settings_target_label`, всюди), прибрано `balance_target_label`
      (більше нема підпису на засічці) і `balance_offline_label` (більше нема єдиного
      "Offline"-блоку), новий `my_day_title_format`, `balance_rest_of_day_label`,
      `category_social`. **Не зроблено в Stage 1 (відомий борг):** Stats-екран (`StatsViewModel.
      refreshWeeklyTrend()`) досі рахує сьогоднішню точку тренду від півночі, не від точки
      старту дня — не входило в узгоджений обсяг Stage 1 (ядро = формула/категорії/Home/
      віджет/ключові слова, Stats — окремий прохід). Повний аудит формулювань поза Home/
      віджетом (Онбординг/Налаштування частково оновлені, Stats/Категорії/Виключені застосунки —
      ні) — FR-P.3–P.6 вимагають цього для ВСЬОГО UI, зроблено вибірково. Зібрано й перевірено
      компіляцією (`./gradlew :app:assembleDebug`), НЕ перевірено вживу на пристрої.
- [x] **Stage 2 — психологічні фічі (FR-P.1, FR-P.2):** `ValuesOnboardingScreen` — питання
      "Що ти хотів би робити більше?" (вибір з 5 активних категорій), показується ЗАВЖДИ
      першим при першому запуску (`SettingsStore.valuesOnboardingSeen`), перевіряється в
      `HomeScreen` окремим `LaunchedEffect`, що виконується РАНІШЕ за (і блокує) ефект показу
      онбордингу доступу до статистики (FR-7.1) — без цього обидва могли б навігувати
      одночасно на першому запуску. "Пропустити" доступний (SRS текст не згадує його явно для
      цього питання, але не забороняє — узгоджується з монастирською філософією "ніколи не
      блокувати"). Відповідь (`valuedCategoryId`) поки лише зберігається, "призма" на
      Stats/Home НЕ реалізована в цьому заході (за узгодженням — мінімальний перший прохід).
      `WeeklyReflectionCard`/`WeeklyReflectionViewModel` — інлайн картка на Home над "Мій
      день", з'являється раз на 7 днів (`SettingsStore.lastReflectionHandledAtMillis`,
      точка відліку сідиться при першому запуску `TeperaApp.onCreate()` — не 0/"ніколи", інакше
      картка з'явилась би одразу, коли ще нема тижня даних): питання + три кнопки-діапазони
      (До 10/10-20/Понад 20 год) + "Можна пропустити"; вибір діапазону показує реальне число за
      останні 7 днів (`BalanceRepository.getOnlineMinutes`) без "вище/нижче" — лише дві цифри
      поруч (FR-P.6); і відповідь, і skip позначають тиждень опрацьованим. Без доступу до
      статистики картка не з'являється (нема реального числа для порівняння).
      Перевірено вживу на Samsung S23: values-онбординг показується першим на чистому
      встановленні (`pm clear`), відповідь веде на Home; тижнева картка (тимчасово прискорений
      інтервал для тесту, повернуто на 7 днів перед комітом) показала запитання, після вибору
      "10-20 год" — "Твоя оцінка: 10-20 год" / "Насправді: 44 год 45 хв", "Гаразд" ховає картку
      до наступного тижня; logcat без крашів.
- [x] **Борг закрито: Stats-екран переведено на точку старту дня.** `StatsViewModel` тепер
      отримує `SettingsStore`, рахує `balanceRepository.calculateDayStartMillis(sleepWindowEndHour)`
      один раз за оновлення й використовує його — замість `startOfTodayMillis()` — і для меж
      підрахунку Online-хвилин, і для знаменника Grace Period Buffer сьогоднішньої (daysAgo == 0)
      точки тренду; минулі завершені доби лишаються на календарних межах [північ, наступна
      північ) зі знаменником 1440 хв (Grace Buffer там не застосовний). Точка на осі X графіка
      свідомо лишається прив'язаною до календарного дня, не до фактичної точки старту —
      інакше сьогоднішня точка "тижневого тренду" зсувалась би вбік від решти днів тижня.
      `StatsScreen`/`TeperaNavHost` прокидають `settingsStore` до `StatsViewModel.Factory`.
      Перевірено вживу на Samsung S23: сьогоднішня точка тренду (~32%) відповідає новій
      формулі (225 хв Online / 705 хв від точки старту дня о 06:06), а не старій (~21% від
      півночі за ті самі вхідні дані); logcat без крашів.

## Фаза 7 — QA та підготовка до релізу (~3 тижні)
- [ ] Тестування на реальних пристроях (Samsung S23, Moto G84, Huawei P9, Sony XZ1 Compact)
- [ ] Багфікс
- [ ] Privacy Policy на GitHub Pages (з явним розкриттям UsageStatsManager)
- [ ] Data Safety form (окремо розкрити доступ до статистики використання застосунків)
- [ ] Content Rating Questionnaire, Target audience
- [ ] Store listing: іконка, feature graphic, скріншоти, описи UA/EN
- [ ] Play App Signing — перевірити (мало бути зроблено у фазі 0)

## Фаза 8 — Закрите тестування Google Play (14+ днів)
- [ ] Рекрутинг 12+ тестувальників заздалегідь (Threads-аудиторія)
- [ ] 14 безперервних днів опт-ін

## Фаза 9 — Реліз
- [ ] Подача на публічний реліз
