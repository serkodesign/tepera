# Software Requirements Specification
## Tepera

| | |
|---|---|
| Версія документа | 3.1 |
| Дата | 21.09.2026 |
| Статус | Узгоджено / В розробці |
| Автор | AI PM (спільно зі стейкхолдером) |
| Стейкхолдер / Product Owner / Solo Developer | Serhii |

### Історія версій
- **v1.0** — базовий SRS за результатами перших уточнювальних сесій
- **v1.2** — редакція стейкхолдера: назва застосунку, вікова аудиторія (20-45), деталізовані acceptance criteria, обробка перекриттів часу, розширена модель даних
- **v1.3** — відновлено Out-of-Scope, критерії успіху, ризики, відкриті питання; додано accessibility та архітектурну готовність; виправлено неточність версії Android для Health Connect
- **v1.4** — доданий Google Play Publishing Checklist (targetSdk 36, AAB, Play App Signing, content rating, target audience, store listing)
- **v1.5** — вирішено 3 останні мікро-питання: applicationId, FR-1.4 (overlap лише в межах категорії), FR-2.4 (лише архівація категорій, без справжнього видалення)
- **v1.9 (зовнішній чернетковий документ, не в нашій послідовності версій)** — альтернативна концепція від стейкхолдера: Trampoline UX / Monastic Style філософія, Online-vs-Offline баланс через UsageStatsManager, Jetpack Glance-віджет, Health Connect/Sleep API перенесено на v2.0
- **v2.0** — синтез: прийнято концепцію Online/Offline балансу з v1.9, віджет на Jetpack Glance (без live-таймера), Health Connect/Sleep API перенесено на post-MVP; **збережено** з нашої лінії — Vico-графіки та історія в MVP (не text-only), 5 категорій включно зі Сном + 1 користувацька, архівація замість видалення (FR-2.4), overlap-перевірка лише в межах категорії (FR-1.4), усі раніше прийняті архітектурні та Google Play compliance рішення
- **v2.1** — фінальна назва застосунку **Tepera** (від укр. "тепер/зараз" — миттєва присутність, узгоджується з Trampoline UX), обрана після кількох раундів брейнштормінгу й перевірки конфліктів з існуючими застосунками (відхилено: LifeBalance/Life Balance як власна назва, Tysh, Roots/Rooted, Ballast, Haven, Anchorage, Foothold, Sundial, Meridian, Equinox, Presently, Nyni, SideQuest, Teper, Cushion — усі зайняті або з небажаними асоціаціями); `applicationId` оновлено на `com.serkodesign.tepera`
- **v2.2** — переосмислено ядро продукту на основі доказової бази: модель "Online vs Offline" замінено **чотирисегментною структурою доби** (Сон / Online / Залоговане офлайн / Решта дня), бо протиставлення автоматично вимірюваного Online і вручну логованого Offline структурно гарантувало провину; додано **розділ 4 "Психологічні принципи подачі"** з FR-P.1–P.6 (механізм "оцінка → реальність", онбординг про цінності, заборона мотиваційних цитат і моралізації); точка старту дня прив'язана до першого розблокування замість 00:00; критерії успіху розгорнуто до трирівневої моделі з явним застереженням щодо реалістичних очікувань
- **v2.3** — зафіксовано як post-MVP кандидат **FR-G "Ворітна" іконка** (розділ 10.2): самостійна затримка відкриття обраних застосунків через `requestPinShortcut`, свідома альтернатива відхиленому підходу з AccessibilityService/overlay (ризики Google Play review, конкуренція з домінуючим гравцем One Sec)
- **v2.4** — модель структури доби спрощена з чотирьох до **трьох сегментів**: сон прибрано з видимого подання (без Sleep API вікно 00:00-06:00 було здогадкою, поданою як факт), лишається лише як технічне налаштування межі початку дня; категорія Сон прибрана з дефолтних, Живе спілкування повернуто на звільнений слот; довжина дня тепер подається як зростаюча величина ("Твій день триває 9 год 40 хв"), не фіксована доба; FR-P.1 (тижнева рефлексія) оновлено на вибір з діапазонів замість відкритого поля; додано розділ 4.4 з описом референсного low-fidelity макета (головний екран, віджет 4×2, картка рефлексії), створеного для перевірки принципів розділу 4 на конкретних елементах UI
- **v2.5** — прибрано трактування дизайну як окремої майбутньої фази "чекаємо Figma": UI реалізується одразу за вже встановленим робочим стилем (розділ 4.3–4.4), без Material 3 плейсхолдера в коді; окрему фазу "Інтеграція дизайну з Figma" прибрано з таймлайну (розділ 8), Figma MCP-конектор лишається доступним інструментом на майбутнє, але не блокує розробку
- **v2.6** — ключовий продуктовий зсув після чесного аналізу ринку: виявлено, що Monastic Style прибрав механізми утримання нічим їх не замінивши, а ручне логування створювало асиметрію зусиль (Online — автоматично, Offline — працею). Доданий **розділ 3.8**: детекція пауз від 30 хв через `queryEvents()`, межі дня ("твій день з телефоном"), тепловий часовий патерн, стек із максимум 3 контекстних карток. Ручне логування лишається, але **перестає бути єдиним способом вводу** (розділ 3.1). Продукт переходить від "ти ведеш журнал" до "телефон знає форму дня — назви кілька шматків"
- **v2.7** — доданий пункт налаштувань "Запропонувати функцію" (FR-6.4–6.6): Google Form, що відкривається в браузері через `ACTION_VIEW`, **без додавання дозволу `INTERNET`**; розміщення під резервним копіюванням; свідомо без промптів, бейджів і прохань оцінити застосунок
- **v2.8** — метрика "твій день з телефоном" **прибрана** (FR-D.6): назва бреше (проміжок читається як час використання), число велике без важеля для впливу — провина без агентності; межі дня тепер видно на тепловому графіку самі собою. Натомість додана метрика **часу останнього використання телефону** (FR-D.7): просто час, без висновків, з межею віднесення пізніх сесій до 02:00. Зафіксована **черга контекстних карток за пріоритетом** (FR-D.10a) і заборона наповнювача в порожніх слотах (FR-D.10b)
- **v2.9** — **FR-P.4 переписано** за рішенням стейкхолдера: повноцінна база знань з темами цифрового детоксу, думскролінгу й нейробіології звички дозволена, але **виключно як контент "на вимогу"** в окремому розділі під "Ще" (FR-P.4a забороняє його появу в картках, структурі дня, порожніх станах і сповіщеннях; FR-P.4b вимагає не стверджувати попнаукові рамки на кшталт "дофамінової залежності" беззастережно). Зафіксована поведінка тапу по віджету: таймер з автозупинкою через 4 год (FR-4.7)
- **v3.0** — доданий **розділ 13 "Прогалини покриття"** (GAP-1…GAP-11), виявлені при побудові карти екранів і звірці сценаріїв. Критичні: відсутній крок онбордингу про віджет (головна щоденна взаємодія, про яку ніщо не розповідає), необроблене відкликання дозволу постфактум, відсутній екран редагування запису попри обіцянку FR-1.4. Розділ публікації в Google Play перенумеровано на 14
- **v3.1 (поточна)** — синхронізація вимог із фактичною реалізацією (звірка з кодом і журналом рішень станом на 21.09.2026). **Нові розділи:** 3.9 "Ворота" (FR-G, із post-MVP перенесено в реалізоване), 3.10 "База знань", 3.11 "Підтримка розробника та Tepera Pro" (платіжний шар через RevenueCat — MVP більше не «без платежів»), 4.5 "Дизайн-система". **Змінено:** 6 дефолтних категорій і до двох власних (FR-2.1–2.5); вікна сну як технічний параметр (до 2, FR-3.2); межа "логічної" доби Home/віджета о 01:00 (FR-3.9); віджети 1x1/2x1/4x1/4x2 без фону в стилі капсули, вибір і порядок кнопок у налаштуваннях (FR-4.1, FR-4.8–4.10); таймер: одночасно один, замість автозупинки — одне сповіщення "усе ще цим займаєшся?" (FR-4.7); Статистика: "Вчора" замість "День", хронологія доби, паузи, "Офлайн" за об'єднанням Online й записів без ночі, підпис "<5 хв" (FR-5.2, 5.6–5.9); Щоденник на 14 діб (FR-5.4); секунди коротших за хвилину таймерів більше не губляться (FR-1.7); механізми "оцінка → реальність" розширено на онбординг, останнє використання й розблокування, рушій карток із бюджетом частоти (FR-D.12–D.16). **Закрито прогалини** GAP-1, 3, 4, 6, 7, 10, 11 (розділ 13.5). Позначки "перевірено на пристрої" в цьому документі означають перевірку на Samsung S23, якщо не вказано інше

---

## 1. Вступ

### 1.1 Мета документа
Визначити повний обсяг функціональних і нефункціональних вимог до MVP мобільного застосунку — трекера офлайн-активностей і структури доби, — достатній для самостійної розробки одним розробником у форматі vibecoding.

### 1.2 Область застосування
Android-застосунок **Tepera** (`applicationId`: `com.serkodesign.tepera`), перший реліз у Google Play. iOS — окремий проєкт після стабілізації Android MVP.

### 1.3 Візія продукту
Tepera будується на двох принципах дизайну (прийнято з v1.9):

- **Trampoline UX (ефект трампліна):** взаємодія із застосунком — надшвидка (1-2 тапи) фіксація активності, після чого користувач одразу повертається до реального життя. Застосунок свідомо не намагається утримати увагу.
- **Monastic Style (монастирський стиль):** спокійний UI без агресивних бейджів, без push-сповіщень про "стріки", без червоних лічильників і ігрофікованого тиску. Фокус на спокійному усвідомленні балансу, не на змаганні із собою.

Ці принципи узгоджуються з уже прийнятим "без соціального шару, без гейміфікації" (SRS v1.x) і поширюються тепер і на UI-мову віджета та головного екрана.

### 1.4 Визначення та скорочення
- **MVP** — Minimal Viable Product
- **Структура доби** (раніше — "Life Balance", потім "Online/Offline баланс") — подання неспаного дня трьома сегментами: Online, "Ти відмітив", Решта дня (розділ 3.3, оновлено у v2.4 — сон більше не окремий сегмент). Свідомо **не** протиставлення двох величин на одній шкалі
- **Решта дня** — сегмент незалогованого неспаного часу (дорога, готування, розмови). Нейтральна, найбільша частина доби, ніколи не подається як прогалина
- **Grace Period Buffer** — математичний буфер у формулі балансу, що уникає викривлення співвідношення на початку доби
- **Exclusion List** — список застосунків, чий час використання не враховується як "Online"-залежність (напр. навігація, е-книги, банкінг)
- **UsageStatsManager** — системний Android API для читання статистики використання застосунків
- **Vibecoding** — розробка в парі з AI-асистентом

### 1.5 Джерела вимог
Серія уточнювальних сесій зі стейкхолдером + зовнішній чернетковий документ v1.9, узгоджений по пунктах перед злиттям (розділ відповідних рішень нижче).

---

## 2. Загальний опис

### 2.1 Перспектива продукту
Самостійний offline-first Android-застосунок у ніші slow-living / digital detox. Позиціонування частково зміщується ближче до категорії "трекери екранного часу" через використання даних екранного часу — це усвідомлений компроміс (див. розділ 11, ризик конкурентного позиціонування), компенсований відсутністю блокування (лише візуалізація й усвідомлення, без примусових обмежень) та категорійним трекінгом офлайн-активностей, якого немає в прямих конкурентів.

### 2.2 Функції продукту (короткий огляд)
- Автоматична детекція пауз, меж дня й часових патернів з даних `UsageStatsManager` (розділ 3.8)
- Ручне логування офлайн-активностей за категоріями — доповнює автоматику, не є єдиним способом вводу
- Розрахунок і візуалізація структури доби за трьома сегментами (Grace Period Buffer, налаштовуваний орієнтир, Exclusion List)
- Адаптивні домашні віджети (Jetpack Glance: 1x1, 2x1, 4x1, 4x2) із вибором і порядком кнопок категорій + Quick Settings tile
- Щоденник (записи за 14 діб) і Статистика "Вчора"/"Тиждень": хронологія доби, паузи, розподіл по категоріях, тижневий тренд (Vico)
- Керування категоріями (6 дефолтних, до двох власних, архівація/повернення з архіву)
- "Застосунки з затримкою" — самостійна пауза перед відкриттям обраних застосунків через закріплений ярлик (розділ 3.9)
- "База знань" — пояснювальний контент "на вимогу" (розділ 3.10); добровільна підтримка розробника (розділ 3.11)

### 2.3 Класи користувачів
Дорослі 20-45 років, які відчувають цифрове вигорання і прагнуть бачити баланс між екранним і реальним часом — без блокування, без гейміфікації.

### 2.4 Модель монетизації
MVP безкоштовний і без реклами; **функціонал застосунку нічим не гейтиться**. **Змінено у v3.1:** за прямим рішенням стейкхолдера додано платіжний шар **лише через RevenueCat** (прямого Play Billing у застосунку немає): добровільна підтримка розробника ("Пригостити кавою") і інфраструктура Tepera Pro (підписки/lifetime), яка поки нічого не блокує і прихована прапорцем на фазі запуску (розділ 3.11). Реклами, платних функцій і пейволу, що з'являється сам, немає

### 2.5 Середовище експлуатації
- Android 8.0 (API 26) — мінімальна підтримувана версія (`minSdkVersion`)
- Android 16 (API 36) — обов'язковий `targetSdkVersion` для Google Play з 31.08.2026
- Мови: українська (основна), англійська — з першого релізу
- **Health Connect / Sleep API — перенесено на post-MVP (v2.0 продукту, не плутати з версією цього документа).** У поточному MVP Рух/Спорт логується вручну як і решта категорій; сон не логується взагалі й не показується як сегмент (розділ 3.3)

### 2.6 Обмеження дизайну та реалізації
- Розробка одним розробником-новачком, vibecoding, ~2 год/день
- UI-стилі слідують уже встановленому робочому стилю (розділ 4.3–4.4); Figma MCP-конектор підключено для подальшого перенесення деталізованого дизайну, коли він знадобиться, але це не блокує розробку
- Без бекенду — offline-first; єдиний виняток — системний Android Auto Backup
- **Один Gradle-модуль**, без KMP-структури в MVP. Хоча v1.9 згадує "KMP-ready" — формальну мультиплатформну структуру не додаємо зараз (конфлікт із рішенням "без мультимодульності" для новачка); дані вже проєктовані платформо-нейтрально (UUID-рядки, прості типи), тож перехід на KMP при старті iOS-фази буде дешевшим, ніж передчасна інфраструктура зараз

### 2.7 Припущення та залежності
- Google Play Console (новий особистий акаунт, ще не створений) вимагає закритого тестування — 12+ тестувальників, 14 безперервних днів опт-ін
- Health Connect і Sleep API — залишаються в архітектурних планах на post-MVP, конкретні NFR/FR щодо них не деталізуються в цьому документі
- Privacy Policy — публікується на GitHub Pages (URL, не PDF)
- **UsageStatsManager вимагає доступу через Налаштування → Спеціальний доступ → Доступ до статистики використання** — не звичайний runtime-діалог дозволу. Застосунок має чітко пояснити цей крок при онбордингу, оскільки без нього ядро функціоналу (сегмент Online у структурі доби) не працює

---

## 3. Функціональні вимоги

### 3.1 Модуль "Ручне логування активності"

**Змінено у v2.6:** ручне логування лишається повноцінним, але **більше не є головним і єдиним способом вводу** — основну частину структури дня застосунок виводить сам з даних `UsageStatsManager` (розділ 3.8). Ручний ввід потрібен для того, чого телефон не може знати: чим саме була пауза, і для активностей, під час яких телефон використовувався.
| ID | Вимога | Деталізація |
|---|---|---|
| FR-1.1 | Додавання запису | Категорія, тривалість, дата/час початку, опційна нотатка (до 250 символів) |
| FR-1.2 | Способи вводу часу | Пресети (+15/+30/+60/+120 хв), ручний ввід тривалості, інтервальний вибір (початок/кінець) |
| FR-1.3 | Обробка перекриттів часу | Перевірка лише в межах ОДНІЄЇ категорії (FR-1.4 з v1.5) — діалог заміни/скасування. Різні категорії можуть перекриватись без попередження |
| FR-1.4 | Керування записами | Перегляд історії (Щоденник, історія категорії), редагування будь-якого поля на екрані редагування (категорія, час, тривалість, нотатка), видалення запису з підтвердженням (саме запису, не категорії). **Закриває GAP-3.** Перевірено на пристрої |
| FR-1.5 | Автоматичний трекінг (Health Connect / Sleep API) | **Перенесено на post-MVP.** У MVP Рух/Спорт логується вручну, як і решта категорій; сон не входить до категорій ручного логування (розділ 3.3) |
| FR-1.6 | Тап-таймер (v3.1) | Тап по картці категорії на Home (або кнопці віджета) запускає живий таймер, повторний тап по тій самій категорії зупиняє й зберігає запис (без діалогу перекриття — швидка дія). **Одночасно йде лише один таймер:** старт нової категорії спершу зупиняє й зберігає попередню. Цілі хвилини зараховуються лише після того, як минули повністю. Активна категорія позначається кольоровою рамкою, без живого лічильника (Trampoline UX). Перевірено на пристрої |
| FR-1.7 | Секунди коротші за хвилину (v3.1) | Таймер коротший за 1 хв і "хвіст" довшого (2:40 → 2 хв у записі + 40 с) не губляться: їх секунди накопичуються в лічильнику доби на категорію (DataStore, не Room — без міграції; не входять в експорт/імпорт). Запис із 0 хв **не створюється**, тож Щоденник і історії не змінюються. Лічильник враховується лише в Статистиці — FR-5.8. Перевірено на пристрої |
| FR-1.8 | Ручний ввід (v3.1) | Екран додавання за макетом Figma: сітка категорій із реальних активних категорій, три режими тривалості (пресети / вручну хвилинами / інтервал), вибір дати; при додаванні з картки категорії сітка прихована, категорія передвибрана. Кнопки "Скасувати"/"Зберегти" закріплені внизу |

### 3.2 Модуль "Категорії"
| ID | Вимога | Деталізація |
|---|---|---|
| FR-2.1 | Дефолтні категорії | **6 категорій** (змінено у v3.1): Природа, Читання, Хобі/Творчість, Рух/Спорт, Живе спілкування, **Справи** (нейтральний "катч-ол" для роботи, дороги й побуту — Справи 6-та, бо п'ять попередніх самі по собі норма, а FR-P.5 забороняє норми). Сон прибрано з категорій ручного логування (v2.4, розділ 3.3); стара категорія Сон архівується при кожному запуску. Кольори дефолтних категорій — з палітри застосунку: Читання #D28FDF, Хобі #00D8CD, Рух #FD5B5E, Природа #00B938, Живе спілкування #FF73D0, Справи #A362FF |
| FR-2.2 | Користувацькі категорії | Можливість додати **до двох** власних категорій (змінено у v3.1 з однієї; назва, колір, іконка з набору) — обмежена кількість, на відміну від необмеженої кастомізації в ранніх версіях. Створення також доступне кнопкою "+ Додати" поруч із заголовком "Активності" на Home; на ліміті кнопка виглядає неактивною, а тап пояснює причину |
| FR-2.3 | "Видалення" категорії | **Змінено у v3.1:** дефолтні категорії — лише архівація (`isHidden = true`); **власні можна видалити по-справжньому** (звільняє слот ліміту). Причина розходження: дефолтні пересіваються при кожному запуску за фіксованим `id`, тож справжнє видалення "воскресло б" |
| FR-2.4 | Повернення з архіву (v3.1) | Архівацію можна скасувати (`unarchive`): у Налаштуваннях → Категорії перемикач архів/розархівувати. Повернення з архіву не звільняє й не займає слот власної категорії — лише перемикає видимість |
| FR-2.5 | Вибір категорій в онбордингу (v3.1, T-8) | Окремий крок онбордингу (після питання про цінності): усі 6 дефолтних категорій перемикачами, вимкнена архівується. Нічого зайвого не з'являється на Home і віджеті. Питання про цінності (FR-P.2) не пропонує "Справи" (питання "що хотів би робити більше" не пасує до хатніх справ). Перевірено на пристрої |

### 3.3 Модуль "Структура доби" (ядро продукту, переосмислено у v2.4)

**Три сегменти доби**, не чотири — сон прибрано з видимої моделі. Причина: без Sleep API (post-MVP, розділ 10) вікно сну — це здогадка, подана як факт, і показ вигаданої цифри поруч із двома реально виміряними підриває довіру до всіх чисел. Компроміс, який варто розуміти свідомо: без сну як "подушки" число онлайн-часу виглядає різкіше (та сама тривалість — більший відсоток від коротшого дня). Це прийнятно, доки різкіше число не супроводжується оцінкою (розділ 4).

Три сегменти лишаються протиставленням Online вимірюється автоматично й повно, Offline — лише вручну й вибірково; без нейтрального третього сегмента "Решта дня" офлайн завжди "програвав" би, перетворюючи застосунок на машину провини.

| ID | Вимога | Деталізація |
|---|---|---|
| FR-3.1 | Джерело даних Online-часу | Читання **сирих подій** `UsageEvents` (`MOVE_TO_FOREGROUND`/`MOVE_TO_BACKGROUND`), обрізаних рівно по межах запиту, — не `queryUsageStats(INTERVAL_DAILY)` (той рахує цілі бакети й для часткової доби систематично завищував: 159 хв у Tepera проти 47 хв у системному Digital Wellbeing). За вирахуванням застосунків з Exclusion List **та** лаунчера й клавіатури (визначаються динамічно через `PackageManager`/`InputMethodManager`, не захардкоджені пакети) |
| FR-3.2 | Вікна сну як технічний параметр (T-12, переписано у v3.1) | Сон не показується як сегмент і не заявляється як факт. Але є **до двох вікон сну** (початок+кінець; вікно може перетинати північ або лежати в денному часі — нічна зміна 09:00–16:00), дефолт слот 1 = 00:00–06:00, слот 2 вимкнений (для плаваючого графіка). Вікна впливають лише на розрахунок: початок дня, довжина неспаного дня (Grace Period Buffer віднімає час у вікні), детекція пауз (паузи, що перетинаються з вікном, не детектуються), "Офлайн" у Статистиці. Редагуються в Налаштуваннях → Відстеження. Чиста арифметика вікон (без Context/Room) покрита юніт-тестами. Room-міграція v2→v3 |
| FR-3.3 | Сегмент "Ти відмітив" (раніше — "Залоговане офлайн") | Ручні записи по 5 категоріях (FR-2.1). **Формулювання в UI:** "Ти відмітив", не "Залоговане офлайн" — перше звучить як власна дія людини, друге як звіт (розділ 4.3) |
| FR-3.4 | Сегмент "Решта дня" | Залишок неспаного часу — дорога, готування, розмови. **Ніколи не подається як втрата, прогалина чи докір за незалогованість.** Найбільша, нормальна частина дня |
| FR-3.5 | Точка старту дня | Відлік від першого **суттєвого** розблокування (перший `MOVE_TO_FOREGROUND` будь-якого застосунку). **Найраніший початок — 05:00** (за прямим запитом стейкхолдера): розблокування раніше — нічні перевірки, до 05:00 "день ще не почався". Розблокування, що стартує всередині ввімкненого вікна сну й триває коротше 5 хв, ігнорується (узгоджено: один механізм замість двох порогів у попередніх версіях) |
| FR-3.6 | Grace Period Buffer | `Знаменник = max(180 хв, хвилин_від_першого_розблокування)`. Уникає ефекту "100% провини" в перші години після пробудження |
| FR-3.7 | Подання довжини дня | UI показує довжину дня, що росте разом з реальним часом: "Твій день триває 9 год 40 хв", **не** "Сьогодні: 24 години". День — величина, що триває, не фіксована абстракція з півночі. **Не плутати з прибраною метрикою FR-D.6:** тут ідеться про довжину неспаного дня як знаменник для шкали трьох сегментів, а не про проміжок між першим і останнім дотиком до телефону |
| FR-3.8 | Обробка перекриття Online і "Ти відмітив" | Аудіокнига під час прогулянки потрапляє і в Online, і в "Читання". Сегменти **не подаються як частини одного цілого** (не кругова діаграма), щоб уникнути враження помилки в підрахунку |
| FR-3.9 | Межа доби Home і віджета (переписано у v3.1) | Home і віджет вважають добою **логічну** добу: **до 01:00 ще попередня**. Записи, додані чи зупинені між 00:00 і 01:00, належать попередній добі; Online, точка старту дня й сітка віджета — теж попередньої доби. Перемикання відбувається на Home наживо (перевірка кожні 30 с + перерахунок) і на віджеті — точним разовим завданням WorkManager о 01:00 (точність не секундна: Doze може зсунути на кілька хвилин). **Статистика й Щоденник лишаються на календарній півночі.** Не перевірено наживо через північ — перевірено юніт-тестом межі (`LogicalDayTest`) |
| FR-3.10 | Орієнтир Online-часу | Дефолт 180 хв/день, налаштовується **слайдером 1–8 год** (не текстовим полем; округлення до цілих годин), у UI — "орієнтир", не "ціль". Відображається як тиха засічка (тонка вертикальна лінія, без підпису). **Перевищення не підсвічується** — ні кольором, ні іконкою, ні зміною тексту, ні зміною кольору самої засічки |
| FR-3.11 | Exclusion List | Користувач обирає застосунки, чий час не враховується як Online (карти, читалки, банкінг). Психологічна функція: знімає внутрішнє самовиправдання "але ж це не скролінг!", роблячи число прийнятним для самої людини |
| FR-3.12 | Fallback без дозволу | Без доступу до статистики використання — чітке пояснення, куди йти в Налаштуваннях; офлайн-логування працює повністю |
| FR-3.13 | "Офлайн" у Статистиці — за об'єднанням (v3.1) | Залишок "Офлайн, який не залоговано" = **неспана доба** (від точки старту дня до кінця доби, мінус вікна сну, FR-3.2) мінус **об'єднання** інтервалів Online й ручних записів. Перекриття (запис поверх Online, записи різних категорій між собою, як в аудіокнизі під час прогулянки — FR-3.8) віднімається **один раз**, а не двічі; нічний Online не ховається (рядок "Online" лишається за всю добу). Раніше залишок рахувався як "доба − Online − сума записів": віднімав перекриття двічі й включав ніч. Приклад: вчора записи перекривались на 1 год 41 хв — старий "Офлайн" 11:30, за об'єднанням і без ночі — 9:15. **Home "Офлайн-життя" поки на старій формулі** — відкрите питання (розділ 12) |

### 3.4 Модуль "Віджет та швидкий доступ"
| ID | Вимога | Деталізація |
|---|---|---|
| FR-4.1 | Віджети (Jetpack Glance, переписано у v3.1) | Чотири розміри: **1x1** (1 кнопка), **2x1** (2 кнопки), **4x1** (до 5 кнопок), **4x2** (5 кнопок + картка сітки доби 12×4 клітинки по 30 хв від календарної півночі; клітинки: до старту дня, кольори категорій, Online, минуле без даних, майбутнє). Кнопка — коло 60dp, hit-box більший за 48x48dp. 4x1/4x2 через `SizeMode.Responsive` (реальну висоту Glance не повідомляє — брейкпоінт 205dp для 4x2), 1x1/2x1 через `SizeMode.Exact` (на Samsung 1x1 дає лише 76dp — капсула стискається, щоб не обрізатись). Кнопки інтерактивні: той самий тап-таймер, що на Home (FR-1.6). Перевірено на Samsung S23; Huawei P9, Motorola G84 у новому стилі не перевірялись |
| FR-4.2 | Без live-таймера | **Рішення v2.0:** на відміну від чернетки v1.9, віджет НЕ показує таймер у реальному часі під час активної дії — статичний стан з періодичним оновленням (FR-4.3). Спрощення заради обсягу для першого проєкту |
| FR-4.3 | Періодичне оновлення даних | WorkManager, ~30 хв (WidgetUpdateWorker) — оновлює всі чотири віджети без відкриття застосунку; **плюс** разове точне оновлення о 01:00 при перемиканні доби (FR-3.9). Дані віджета — живі потоки всередині композиції (не знімок до `provideContent`), тож кілька екземплярів віджета не розходяться |
| FR-4.4 | Quick Settings tile | Швидке додавання запису зі шторки Android: плитка відкриває екран додавання запису (там категорія обирається, тож **закриває GAP-6**) |
| FR-4.7 | Поведінка тапу по категорії на віджеті (**переписано у v3.1**) | Тап запускає таймер, повторний тап зупиняє й створює запис (FR-1.6); одночасно лише один. **Автозупинки НЕМАЄ** (скасовано рішення v2.9 за прямим запитом стейкхолдера: "таймер не зупиняй, а лиш показуй сповіщення"). Натомість через 4 год після старту — **одне сповіщення** "усе ще цим займаєшся?" (WorkManager, звіряє, що це той самий сеанс) із діями "Так" (просто закриває, без повторів — Monastic Style) і "Ні" (зупиняє й зберігає запис, як звичайний тап). Запит `POST_NOTIFICATIONS` — рівно раз (FR-7.4). Перевірено на пристрої. **Закриває GAP-4** (записів "тривалість під питанням" більше не створюється) |
| FR-4.5 | Кнопки за замовчуванням | Якщо користувач не обрав власний список (FR-4.8), порядок кнопок змінюється за часом доби (ранок/день/вечір); власні категорії — в кінці |
| FR-4.6 | "5-й слот" — підказка про давно не логовану категорію | **Прибрано** за прямим запитом стейкхолдера: усі неактивні кнопки виглядають однаково, незалежно від того, коли категорію логували востаннє |
| FR-4.8 | Налаштування кнопок віджета (v3.1) | Налаштування → Активності → "Кнопки віджета": **один спільний впорядкований список** для всіх розмірів — 1x1 бере першу, 2x1 — перші дві, 4x1/4x2 — до п'яти. Додавання тапом, прибирання "×", порядок — перетягуванням за ручку ліворуч від іконки. Порожній список = автоматичний порядок (FR-4.5). Архівовані/видалені категорії мовчки випадають. Зміни одразу оновлюють віджети. **Закриває GAP-7.** Перевірено на пристрої (перетягування — на списку з двох позицій) |
| FR-4.9 | Вигляд віджетів (v3.1) | За Figma "App concept": **без фону віджета**, кола лежать на шпалерах. Ряд кнопок — напівпрозора **капсула** (#FFFFFF@30%, радіус 100, відступ 12dp). Невибрана кнопка — біле коло + сірий контурний гліф; вибрана (йде таймер) — коло відтінку категорії + темний заповнений гліф того ж відтінку (для власних категорій — їхній колір із прозорістю 25% і затемнений гліф). Гліфи — 1:1 з SVG макета. 4x2: під капсулою напівпрозора картка сітки. Іконка застосунку — за Figma (node 234:435) |
| FR-4.10 | Пропозиція віджета в онбордингу (v3.1) | Останній крок онбордингу, показується рівно раз для обох гілок "доступ надано так/ні": пояснення + системний запит закріплення віджета (`requestPinAppWidget()` на диспетчері виклику), "Пропустити" завжди доступне. **Закриває GAP-1.** Перевірено на пристрої (S23) |

### 3.5 Модуль "Статистика та історія"
| ID | Вимога | Деталізація |
|---|---|---|
| FR-5.1 | Головний екран | Зверху — стек контекстних карток (FR-D.10), під ними структура доби за трьома сегментами + довжина дня, що росте (FR-3.7) + список сьогоднішніх записів з можливістю швидкого видалення. Числа подаються з контекстом, не голим відсотком (FR-P.6) |
| FR-5.2 | Період статистики (переписано у v3.1) | Перемикач **"Вчора" / "Тиждень"** (раніше День/Тиждень/Місяць). **"Вчора" — завершена календарна доба**, не сьогодні (сьогодні вже є на Home і в Щоденнику). Тиждень — ковзне вікно 7 діб, включно із сьогодні. Місяця в MVP немає |
| FR-5.3 | Історична візуалізація | Тижневий тренд Online (власний Canvas-графік, абсолютні години, вісь 0–12 год і більше за потреби). Порівняння лише з власною історією користувача, ніколи з нормою (FR-P.5). Для "Вчора" тренду нема (одна точка) |
| FR-5.4 | Щоденник (v3.1) | Вкладка нижнього навбару: записи за **14 діб** (сьогодні, вчора, далі — за датою; порожні дні не показуються), у кожного дня — розблокування там, де є системна історія (до ~7–9 діб; старіші не показуються — нуль був би хибним фактом), "востаннє брав телефон" лише під "Вчора". Редагування запису — олівець, додавання — кругла кнопка "+". Записи самі не видаляються — глибина 14 діб лише глибина показу. Перевірено на пристрої |
| FR-5.5 | Історія категорії (v3.1) | Тап по тілу картки категорії на Home відкриває повну історію ЦІЄЇ категорії (глибина необмежена) з переходом до редагування запису. "База знань" — окремо (розділ 3.10) |
| FR-5.6 | Статистика "Вчора": склад екрана (v3.1) | Зверху вниз: чипи меж дня ("Початок", "Востаннє брав телефон учора", "Розблокувань учора" — деталі дня за T-14/T-10, не на Home); **хронологія доби** (48 клітинок по 30 хв, пріоритет: запис категорії > Online > пауза без телефону > "офлайн без запису" > невідомо; клітинки до старту дня й у вікні сну нейтральні) з тихим рядком порівняння Online із власною типовою добою (медіана до 7 попередніх діб, лише доби, що повністю в системній історії, показується від 3 днів); картка **"Паузи без телефону"** (кількість, найдовша з категорією, якщо позначена); розподіл часу по категоріях; патерн екрану за вчора. Без слів-оцінок, без traffic-light, без топу застосунків (свідоме рішення: найближче до "оцінки"). Перевірено на пристрої |
| FR-5.7 | Статистика "Тиждень": склад екрана (v3.1) | Чипи "Розблокувань за тиждень" і "Медіана останнього дотику" (T-14/T-10; API < 28 — приховано, а не "0"); розподіл часу по категоріях (сума за 7 діб); тренд Online; патерн (середнє на годину). Перевірено на пристрої |
| FR-5.8 | Малі суми в розподілі (v3.1) | Категорія із сумою за період (цілі хвилини записів + накопичені секунди FR-1.7) **менше 5 хв** показується як **"<5 хв"** з тонкою смужкою; від 5 хв — числом; до 15 хв — точні хвилини (заокруглення до чверті години давало б "0 хв" для 5–7 хв); від 15 хв — з округленням до чверті. Нуль не показується взагалі. Правило однакове для будь-яких записів (довгих, коротких, ручних). Перевірено на пристрої |
| FR-5.9 | Навігація (v3.1) | Нижній навбар-"таблетка" з трьома вкладками — **"Сьогодні" (Home) / "Огляд" (екран "Щоденник") / "Дані" (екран "Статистика")** (кнопки "+" по центру нема — додавання часу per-категорійне, кнопка "додати час" на картці; у Щоденнику — окрема "+"). Налаштування — шестерня на Home, "База знань" — іконка книги на Home. Вкладки перемикаються стандартним патерном `popUpTo(start){saveState}`+`restoreState` |


### 3.6 Модуль "Дані та резервне копіювання"
| ID | Вимога |
|---|---|
| FR-6.1 | Android Auto Backup увімкнено за замовчуванням |
| FR-6.2 | Ручний експорт/імпорт у JSON | Окремий екран Налаштування → "Резервне копіювання". Імпорт вимагає підтвердження ("Замінити всі дані?"); при помилці файлу — спокійне повідомлення "Не вдалося імпортувати — перевірте файл", наявні дані не змінюються. **Закриває GAP-10.** Що НЕ входить в експорт: лічильники секунд FR-1.7 і поточні налаштування DataStore (окрім описаного в моделі даних) |
| FR-6.3 | Повністю offline-first, мережевий доступ не потрібен |
| FR-6.4 | Пункт "Запропонувати функцію" | Розміщується в налаштуваннях **безпосередньо під резервним копіюванням**. Тап → короткий діалог, що чесно попереджає: зараз відкриється браузер, форму приймає Google → підтвердження → `Intent.ACTION_VIEW` на Google Form. **Дозвіл `INTERNET` НЕ додається** — застосунок лише передає намір системі, мережею користується браузер зі своїм дозволом. Це зберігає чистоту "нічого не виходить із пристрою" в маніфесті й Data Safety |
| FR-6.5 | Поведінка пункту "Запропонувати функцію" | Дві окремі форми під UA/EN. **Без передзаповнення полів через URL-параметри** (версія застосунку й модель пристрою для пропозиції функції не потрібні). Коректна обробка відсутності браузера чи мережі — спокійне повідомлення, не краш. Формулювання нейтральне: "Запропонувати функцію", не "Маєш ідею? Поділись!" |
| FR-6.6 | Чого пункт НЕ робить (розділ 4) | Застосунок **ніколи не просить фідбек сам**: жодних промптів, жодного бейджа на пункті меню, жодного перетворення на "оцініть застосунок". Пункт лежить у налаштуваннях і чекає, поки людина сама захоче |
| FR-6.7 | Мова застосунку (v3.1) | Налаштування → Мова: **Системна / Українська / English**. Перемикач реалізовано власним обгортанням контексту (`SharedPreferences` для синхронного читання в `attachBaseContext` + `Activity.recreate()`), а не `AppCompatDelegate` — той працює лише для `AppCompatActivity`. **Тема — завжди світла** (макети Figma тільки Light; єдиний виняток — темний екран дозволів онбордингу). **Закриває GAP-11** (тема не налаштовується — рішення прийняте) |
| FR-6.8 | Обмеження версії й орієнтації | Портретна орієнтація зафіксована (ландшафт вимкнено) за прямим запитом стейкхолдера |

### 3.7 Модуль "Дозволи"
| ID | Вимога |
|---|---|
| FR-7.1 | Доступ до статистики використання (`PACKAGE_USAGE_STATS`) запитується через темний екран дозволів онбордингу з чеклістом (доступ до статистики, сповіщення), поясненням "навіщо" й тихим "Пропустити" (застосунок працює без доступу — FR-3.12). Порядок першого запуску: цінності (FR-P.2) → вибір категорій (FR-2.5) → питання про онлайн учора (FR-P.1, T-3) → екран дозволів → пропозиція віджета (FR-4.10) → Home. Направляє в системні Налаштування (спеціальний доступ, не runtime-діалог) |
| FR-7.2 | Жодних дозволів на геолокацію (як і раніше) |
| FR-7.3 | Data Safety form і Privacy Policy явно описують доступ до статистики використання застосунків — це найчутливіший дозвіл у MVP (розділ 5.2, розділ 11) |
| FR-7.4 | Дозвіл на сповіщення (v3.1) | `POST_NOTIFICATIONS` (Android 13+) — потрібен лише для сповіщення "усе ще цим займаєшся?" (FR-4.7). Запитується з екрана дозволів онбордингу (перший тап — системний діалог, далі — налаштування сповіщень); на Home лишено запобіжний запит рівно раз для тих, хто екран не бачив. Затримка перед запитом гарантує, що діалог не з'явиться поверх проміжного екрана онбордингу |

---

### 3.8 Модуль "Детекція пауз, межі дня та контекстні картки" (нове у v2.6)

Ключовий зсув продукту: замість "ти ведеш журнал свого життя" — **"телефон уже знає форму твого дня, допоможи назвати кілька шматків"**. Вирішує дві проблеми, виявлені в аналізі ринку: асиметрію зусиль (Online вимірювався автоматично, Offline вимагав ручної праці) і відсутність механізмів утримання після свідомої відмови від стріків/гейміфікації.

**Технічна передумова:** перехід з `queryUsageStats()` на **`queryEvents()`** — агреговані суми не показують проміжків, потрібен потік подій. Це підвищує важливість спайку у фазі 2.

| ID | Вимога | Деталізація |
|---|---|---|
| FR-D.1 | Детекція пауз | Проміжок без активності на передньому плані **тривалістю від 30 хв** у межах неспаного дня вважається паузою-кандидатом. Коротші ігноруються (душ, черга, магазин — шум) |
| FR-D.2 | Чесне обмеження детекції | Пауза ≠ офлайн-активність. Телефон не відрізняє прогулянку від наради, дороги чи роботи за комп'ютером. Детекція **не робить логування повним**, лише легшим там, де воно доречне. Більшість пауз залишиться безіменними — і чесно піде в "Решту дня" |
| FR-D.3 | Вікно опитування | Картка з паузами показується, лише якщо користувач **сам відкрив застосунок** у вікні **22:00–00:00 (паузи сьогоднішнього дня)** або **00:00–10:00 наступного дня (паузи вчорашнього)**. Після 10:00 — не показується. **Жодних push-сповіщень.** Картка явно вказує "сьогодні" чи "вчора", бо це різні набори даних |
| FR-D.4 | Крайовий випадок нічного графіка | **Відоме обмеження:** жорстке вікно 22:00–10:00 не підходить для нічних змін. Якщо налаштоване вікно сну користувача повністю накриває вікно опитування — картка не показується взагалі, паузи лишаються доступними в історії. Переглянути, якщо будуть скарги користувачів |
| FR-D.5 | Позначення паузи | Тап по паузі → вибір категорії з 5 доступних. Позначена пауза стає звичайним записом (`EntrySource.GAP_LABELED`). Непозначені паузи **можна позначити пізніше в історії**, але застосунок про це ніколи не нагадує |
| FR-D.6 | Межі дня — поглинуті хітмепом (**переглянуто у v2.8**) | Окрема метрика "твій день з телефоном" (проміжок від першого до останнього дотику) **прибрана**. Причини: назва бреше — проміжок включає години, коли телефон лежав у кишені, і читається як час використання; число велике, бо день довгий, а важеля для впливу немає — провина без агентності (розділ 4). Межі дня видно **самі собою** на тепловому графіку (FR-D.8), там, де починається й закінчується тепло, без числа й без формулювання |
| FR-D.7 | Час останнього використання телефону (**переписано у v3.1, T-10**) | Постійний показ на Home **скасовано** — він порушував принцип пасивного сорому (розділ 4). Замість цього: картка-оцінка за тим самим форматом "оцінка → реальність", що тижнева рефлексія: питання "о котрій востаннє брав телефон?" із п'ятьма діапазонами (До 22:00 / 22:00–23:00 / 23:00–00:00 / 00:00–01:00 / Після 01:00), потім реальний час поруч і тихий третій рядок — медіана за 7 днів (без підпису й порівняння). Сире число доступне на Статистиці ("деталі дня" — під "Вчора", "тижневий огляд" — медіана). Правила появи: не раніше 7-го дня використання, не частіше разу на 2 тижні, ніколи в тому самому тижні, що FR-P.1, і лише при даних мінімум за 5 із 7 днів; жодна умова не звіряється з фактичним часом (інакше картка стає "адресною доставкою сорому" тим, хто лягає пізно). Не перевірено на пристрої (картки-оцінки не побачені наживо через гейтинг "7 днів") |
| FR-D.7a | Межа віднесення пізнього використання | Сесії **до 02:00** належать попередньому дню, після 02:00 — новому. **Не плутати з вікном сну:** вікно сну (за замовчуванням 00:00–06:00) лишається межею для *початку* дня й детекції пауз; 02:00 — виключно межа *віднесення* пізнього використання. Сесія о 01:42 — це ще вчора, о 02:30 — уже сьогодні |
| FR-D.7b | Формулювання | "Востаннє брав телефон о **01:42**", не "останнє вмикання" — технічно ми бачимо останню сесію на передньому плані, а не вмикання екрана, і назва не повинна обіцяти точності, якої немає. Свідоме рішення: пізнє число соромить себе саме, без коментаря — це той самий принцип, що в механізмі "оцінка → реальність" (факт говорить, застосунок мовчить) |
| FR-D.8 | Часовий патерн (несе також межі дня) | Тепловий графік доби, де видно згущення онлайн-часу **і краї дня** (FR-D.6). **Межа, проведена ножем:** показати патерн — факт (людина робить висновок сама); прокоментувати патерн ("спробуй відкладати телефон о 22:00") — порада, **заборонена** (розділ 4) |
| FR-D.9 | Порожній стан патерну | Поки даних менше тижня: "Тут з'явиться патерн, коли назбирається тиждень даних". Формулювання **не має бути закликом користуватися застосунком** ("користуйтесь принаймні тиждень" — з іншої філософії) |
| FR-D.10 | Контекстні картки | Верх головного екрана: **вертикальний стек, максимум 3 картки** одночасно, за пріоритетом актуальності (час доби, наявність нових даних). **Не карусель** — горизонтальна прокрутка запрошує гортати (dwell time проти Trampoline UX) і ховає вміст за першою карткою |
| FR-D.10a | Черга карток за пріоритетом (оновлено у v3.1) | Слот 1 назавжди за структурою дня. Далі — черга за актуальністю: розкриття "оцінка → реальність" після онбордингу (T-3); непозначені паузи — лише у вікні FR-D.3; тижнева "оцінка → реальність" (FR-P.1); картка-оцінка останнього використання (FR-D.7); картка-оцінка розблокувань (FR-D.13); тижневий дайджест "Цей тиждень" (кількість і час Читання, кількість Рух/Спорт, середня точка старту дня — **без метрик кадансу/стріків**: "Хобі: 3 з 4 тижнів" з мокапу замінено на прості абсолютні лічильники); свідчення компетентності — картка "Цього місяця N раз(и/ів) ти вирішив не зараз" (FR-G.6); мініпатерн доби (пасивна довідка). Кожна картка з даними має "×" — ховає до появи нового вікна даних, не назавжди. Візуального "стеку" з перекриттям (макет Figma) свідомо нема |
| FR-D.10b | Заборона наповнювача | Якщо кандидатів менше трьох — показувати менше карток, **не добивати наповнювачем**. Якщо кандидат один — індикатора-точок немає взагалі. Застосунок не вигадує собі контент, щоб заповнити слоти |
| FR-D.11 | Компактність карток | Картки — одна-дві строки, без ілюстрацій. Три картки не повинні витісняти шкалу структури дня за межі першого екрана: ядро продукту лишається видимим без прокрутки |
| FR-D.12 | Параметризація детекції пауз (T-11, v3.1) | Пресет чутливості в Налаштуваннях (три варіанти без числових полів — числа вимагали б розуміння алгоритму): Рідко / Нормально / Часто. Нормально = 30 хв (FR-D.1), макс. 4 паузи на день. Коротка сесія між двома кандидатами **зливає** їх в одну паузу (а не відкидає), стеля застосовується після злиття (лишаються найдовші). При зміні пресету непозначені паузи перераховуються без перезапуску (позначені/пропущені не чіпаються) |
| FR-D.13 | Лічильник розблокувань (T-14, v3.1) | `KEYGUARD_HIDDEN` (API 28+); нижче — фіча **приховується** цілком, без апроксимації (документ: людина порівнюватиме число з відчуттям і перестане довіряти). Розблокування у вікні сну не рахуються. Число ніколи не подається пасивно (Home, віджет): лише в деталях дня (Щоденник, Статистика) і як періодична картка-оцінка за правилами частоти FR-D.15. Підтверджено реальними даними: 793 розблокування на тиждень, медіана останнього дотику 00:14 |
| FR-D.14 | Бекфіл історії (T-2, v3.1) | Одразу після першого наданого доступу застосунок обробляє всю доступну системну історію (до 7 діб): бекфіл виявлених пауз і тепловий патерн видно вже при ≥2 днях реальної історії (замість очікування штучного тижня від інсталяції). Тихий індикатор "Обробляємо історію…" на Home, поки триває. Це свідомо змінило рішення v2.6 (тиждень від першого запуску) — принцип пріоритету ТЗ |
| FR-D.15 | Рушій карток і бюджет частоти (T-13, v3.1) | Одне місце (чиста Kotlin-логіка, юніт-тести включно з 60-денною симуляцією) визначає, які картки-оцінки показувати: глобальний бюджет — **не більше однієї картки-оцінки на тиждень сумарно**, максимум 3 картки одночасно, "подієві" не витісняють тижневі більш ніж двічі поспіль. Історія показів читається лише рушієм (тренди з неї заборонені). Заміняє окремі ад-хок правила частоти в кожній картці |
| FR-D.16 | Тепловий патерн реагує на період (v3.1) | Патерн доби на Статистиці залежить від перемикача: "Вчора" — годинний розподіл вчорашньої доби, "Тиждень" — середнє Online на годину за 7 діб (середнє, а не сума, щоб довший період не виглядав "гарячішим"). Для "Вчора" поріг мінімальних днів не застосовується. Компактна версія на Home лишається 7-денною |

### 3.9 Модуль "Ворота — застосунки з затримкою" (FR-G, реалізовано у v3.1; раніше post-MVP кандидат 10.2)

Самостійна затримка відкриття обраних застосунків — без AccessibilityService, без overlay поверх сторонніх застосунків і без нових чутливих дозволів (розділ 10.2 задокументував причини відмови від AccessibilityService). Оцінки немає: подія "не зараз" не подається як перемога, "зачекав і зайшов" — не як поразка.

| ID | Вимога | Деталізація |
|---|---|---|
| FR-G.1 | Вибір застосунків (T-4) | Налаштування → "Застосунки з затримкою": список застосунків із реальною історією використання (видимість пакетів — через `<queries>`, без `QUERY_ALL_PACKAGES`). Створення воріт — закріплений ярлик (`requestPinShortcut()` на диспетчері виклику, не на IO) з бейджем-куточком у безпечній зоні adaptive-іконки (перша версія з кільцем по краю зникала в діалозі пінування). Кожне створення генерує **свіжий унікальний `shortcutId`** (Android не дозволяє знімати закріплення, повторне створення з тим самим ID не показувало діалог розміщення). Видалення ярлика з робочого столу видаляє рядок і в застосунку (звірка з `pinnedShortcuts` при відкритті екрана). Інструкція після створення; попередження, що оригінальна іконка ще на екрані; "вимкнути ворота на сьогодні" — один тап без підтвердження. Перевірено на S23 (повний цикл), Huawei P9 (пінування й екран паузи) |
| FR-G.2 | Затримка (v3.1) | Тривалість — **3 / 5 / 10 с**, інтерактивний чіп у списку воріт (тап перемикає по колу й одразу зберігається; діє з наступної паузи, ярлик перестворювати не треба); те саме в діалозі створення (дефолт 10 с). Старі ворота з 20 с першим тапом переходять на 3 с. Перевірено на пристрої (цикл 5→10; 10→3→5 не перевірено) |
| FR-G.3 | Екран паузи (T-5) | Назва застосунку, дихальна анімація ("квітка": росте 4 с, стискається 4 с, незалежно від таймера), зворотний відлік очікування, "Ти намагався відкрити цей застосунок N разів" (свідомо скасовує вимогу T-5 "нічого, крім назви й кнопки" — стейкхолдер підтвердив), кнопки "Продовжити" (контурна, тиха, неактивна й напівпрозора до кінця очікування, перехід лише по тапу — автозапуску нема) і "Вийти" (суцільна, візуально основна). Дебаунс: повторний тап протягом 30 с паузу не показує. "Вийти"/системна "назад" завжди ведуть на робочий стіл. `MainActivity` — `launchMode="singleTask"` + `onNewIntent()` (без цього повторний тап у фоні не доставляв нових extras). Перевірено на S23 і P9 (Animator duration scale = 0 на цьому P9 був причиною "миготіння" — налаштування пристрою, не баг) |
| FR-G.4 | Подія рішення (T-6) | Одна подія на кожне реальне рішення на екрані паузи (`PROCEEDED` / `CANCELLED`); шлях "пропустити паузу" (дебаунс, вимкнено на сьогодні) події не пише — нема моменту вибору. Системна "назад" = те саме, що "Вийти". Перевірено прямим SQL-запитом до `gate_events` |
| FR-G.5 | Обмеження (озвучуються чесно) | Обхід тривіальний (шухляда застосунків, "Нещодавні", сповіщення, оригінальна іконка). Це self-nudge, не блокування. **AccessibilityService-воріт свідомо не робимо** навіть post-MVP без нового явного запиту: суперечить принципу "PACKAGE_USAGE_STATS — єдиний чутливий дозвіл", задокументована історія масових видалень Google Play для app-blocking (2019), суворіший review при додаванні такого дозволу вже існуючому застосунку, технічно слабший результат (спалах цільового застосунку перед паузою) |
| FR-G.6 | Картка компетентності (T-6, FR-P.3) | "Цього місяця N раз(и/ів) ти вирішив не зараз" — рахує лише `CANCELLED` за календарний місяць, без порівняння з `PROCEEDED`; перший `<plurals>` у проєкті; частота — раз на 30 днів через рушій FR-D.15. Перевірено на пристрої |

### 3.10 Модуль "База знань" (FR-P.4, T-16 частина А, реалізовано у v3.1)

| ID | Вимога | Деталізація |
|---|---|---|
| FR-K.1 | Розташування | Іконка-книга на Home і рядок у Налаштуваннях → "База знань". Контент **ніколи** не з'являється на інших екранах (FR-P.4a) |
| FR-K.2 | Контент | Реальний пояснювальний текст (T-16 частина А; конкретний зміст — `ROADMAP.md`, розділ "T-16 частина А"). **Частина Б (вікі про думскролінг/дофамін) заблокована рішенням власника** — попередньо каркас із чотирма заголовками й бейджем "Скоро" |

### 3.11 Модуль "Підтримка розробника та Tepera Pro" (v3.1)

Єдиний платіжний шар — **RevenueCat** (KMP-артефакти `purchases-kmp-core`/`-ui` 3.9.0 у наявному Android-модулі; формального KMP немає). Прямого Play Billing у застосунку немає. Ключ SDK — з `local.properties` (`revenuecat.apiKey.debug` = Test Store, `revenuecat.apiKey.release` = Google Play); Test Store ключ SDK у release відхиляє, тож без release-ключа SDK лишається вимкнений, а екрани показують "недоступно", не падають. Ключ не комітиться.

| ID | Вимога | Деталізація |
|---|---|---|
| FR-M.1 | Підтримка ("Пригостити кавою") | Налаштування → Загальні → "Пригостити розробника кавою". Екран будується від пакетів offering `support` (`support_small|medium|large` мають локалізовані назви), тож кава може стати підпискою зміною продуктів у дашборді. Без offering — "Поки що недоступно". Тихий екран (Monastic Style), без нагадувань і бейджів |
| FR-M.2 | Tepera Pro — інфраструктура | Стан із `CustomerInfo.entitlements["tepera_pro"]` (не з product id): екран Pro, Paywall (`Paywall(PaywallOptions)`, закривається лише через `dismissRequest`), Customer Center, "Відновити покупки". **Pro нічого не гейтить, Paywall сам не з'являється.** Вхід у "Tepera Pro" **прихований на фазі запуску** прапорцем `PRO_ENTRY_ENABLED = false` (екрани лишаються в коді); заглушку "Pro (скоро)" свідомо не робили — видимий неактивний елемент ризикованіший за приховування |
| FR-M.3 | Статус перевірки | Перевірено на S23 через Test Store: конфігурація, offerings, Paywall (дефолтний шаблон), тестова покупка → Pro активний, Customer Center. **Реальну оплату Google Play й release-збірку з `goog_` ключем НЕ перевіряли.** Що лишилось (дашборд, Play Console, Data Safety, політика) — `docs/revenuecat-setup.md` |

## 4. Психологічні принципи подачі (доказова база)

Розділ доданий у v2.2. Визначає, **як** інформація подається — обов'язковий рівень і для UI, що вже реалізується, і для написання всіх текстів інтерфейсу. Порушення цих принципів робить продукт іншим продуктом, навіть за збереження всього функціоналу.

### 4.1 Доказова основа

- **Сором і провина не працюють.** Дослідження iOS ScreenTime показало: функція змінювала поведінку, але викликала стрес, сором і провину, а користувачі **все одно поверталися** до застосунків попри попередження. Провина додає страждання, не зупиняючи поведінку.
- **Змішані категорії провокують самовиправдання.** Дослідження WellScreen (CHI 2026): коли "хороше" й "погане" використання злиті в одну цифру, людина витрачає енергію на внутрішній захист его, а не на усвідомлення. → обґрунтування Exclusion List (FR-3.10).
- **Обмеження викликають реактивність.** Push-фактори, що втручаються в намір користувача, загрожують автономії й ведуть до відторгнення втручання. Примус може бути ефективним рівно доти, доки людина його терпить.
- **Автономія — механізм, що працює довго.** Теорія самодетермінації: стійка зміна залежить від автономії, компетентності й зв'язності. У шкільному дослідженні саме зміни в автономній мотивації опосередкували ефект на екранний час через 18 місяців.
- **Натхнення шкодить, розширення можливостей допомагає.** У дослідженні mHealth-застосунку для відмови від куріння "надихання" користувачів було **негативно** пов'язане з успіхом, тоді як психологічне розширення можливостей (empowerment) — позитивно.
- **Тиха присутність краща за конфронтацію.** У дослідженні переривання скролінгу пряма самоконфронтація (живе відео себе) працювала гірше через збентеження й реактивність, а пасивна відбивна поверхня — найкраще за наміром використання й задоволеністю.

### 4.2 Функціональні вимоги, що випливають

| ID | Вимога | Деталізація |
|---|---|---|
| FR-P.1 | Механізм "оцінка → реальність" | Раз на тиждень (необов'язково, кнопка/жест "Пропустити" завжди видима): "Скільки, на твою думку, ти був онлайн цього тижня?" — **вибір з діапазонів** (напр. "до 10 год" / "10–20 год" / "понад 20"), не відкрите поле вводу — нижчий поріг взаємодії, менше відчуття екзамену. Потім показ реального числа поруч. Рефлексію запускає **власний розрив** людини, а не оцінка застосунку. Найсильніша знахідка з доказової бази |
| FR-P.2 | Онбординг: питання про цінності | Одне питання при першому запуску: "Що ти хотів би робити більше?" (вибір із категорій). Далі статистика подається через призму **власної названої цінності**, а не абстрактної норми |
| FR-P.3 | Свідчення компетентності замість натхнення | Тихі факти власної здатності ("за цей місяць — 14 прогулянок") замість мотиваційних цитат. **Мотиваційні цитати в застосунку заборонені** — доказова база показує негативний зв'язок з успіхом |
| FR-P.4 | Пояснювальний контент — дозволений лише "на вимогу" (**переписано у v2.9**) | **Було:** будь-яка моралізація про шкоду скролінгу заборонена. **Стало:** розрізняємо нав'язану й витягнуту інформацію. Доказова база про реактивність стосується переважно **нав'язаної** — коли застосунок сам, без запиту, повідомляє людині, що її поведінка шкідлива. Контент, який людина **свідомо відкриває сама**, цієї реактивності не викликає. Тому: пояснювальний і освітній контент (включно з темами цифрового детоксу, думскролінгу, нейробіології звички) дозволений **виключно в окремому розділі "Як працює Tepera"** під вкладкою "Ще" |
| FR-P.4a | Межі пояснювального контенту | Контент з розділу знань **ніколи не з'являється**: у стеку карток на Головній, у структурі дня, у порожніх станах, у сповіщеннях, у вигляді "поради дня" чи бейджа. Жодного просування розділу з інших екранів. Людина або сама туди йде, або ніколи його не бачить |
| FR-P.4b | Вимога точності контенту | "Дофамінова залежність" і "дофаміновий детокс" — значною мірою **попнаукові рамки**, а не усталена нейронаука. Контент не має стверджувати їх беззастережно: подавати як поширену метафору з поясненням, що саме показують і чого не показують дослідження. Некоректний контент підриває доказовість, на якій побудований увесь продукт |
| FR-P.5 | Порівняння лише з власною історією | "Довше, ніж твоє середнє за тиждень" — дозволено. "Вище рекомендованого", порівняння з іншими користувачами чи з нормою — заборонено |
| FR-P.6 | Число завжди з контекстом, ніколи відсоток самотньо | "2 год 15 хв з 3 год 20 хв неспання" — факт. "68%" — сприймається як оцінка |

### 4.3 Принципи оформлення UI і текстів

- **Жодного trafic-light кодування.** Червоний/зелений читається як вирок навіть за нейтрального тексту. Палітра тепла, приглушена, не тривожна
- **Аудит усіх текстів на оцінювальні слова.** "Забагато часу онлайн" → "Сьогодні: 3 год 12 хв онлайн". Факт, не оцінка
- **Порожній стан без докору.** Не "Ти нічого не зафіксував", а "Поки порожньо"
- **Уникати слова "ціль"/"таргет" в UI.** "Твій орієнтир" або засічка без підпису — те саме інформаційно, без присмаку невдачі
- **Віджет: шкала тихіша за кнопки логування.** Головна дія віджета — зафіксувати життя, а не подивитись вирок. Число не домінує візуально
- **Сегмент "Решта дня" подається нейтрально-тепло**, ніколи як прогалина в даних
- **Принцип пасивного сорому (ТЗ розділ 2.2, v3.1):** метрика, здатна засмутити (розблокування, останнє використання), ніколи не подається пасивно на Home чи віджеті — лише через власну оцінку користувача (питання → відповідь → факт поруч → тиша) або в деталях дня на Статистиці/Щоденнику. Правило частоти карток-оцінок — рушій FR-D.15
- **Заборонені слова й порівняння розширено:** "ціль/таргет", оцінювальні слова, стріки, "N з M тижнів" (каденс), порівняння з нормою чи іншими людьми. Порівняння лише з власною історією (FR-P.5) і тихим рядком

### 4.4 Референсний макет (низька деталізація, робочий орієнтир для UI)

Створено орієнтовний low-fidelity макет головного екрана, віджета 4×2 й картки тижневої рефлексії — демонструє застосування принципів 4.3 на конкретних елементах UI. Це вже встановлений робочий стиль (не тимчасова заглушка) — реалізація в коді слідує йому, поки не з'явиться підстава його змінити.

**Головний екран:**
- Заголовок дня за FR-3.7: "Твій день триває 9 год 40 хв"
- Горизонтальна шкала трьох сегментів (Online / Ти відмітив / Решта дня), приглушені кольори (блакитний, зелений, теплий сірий — не trafic-light), тонка засічка орієнтиру без підпису й без зміни кольору
- Легенда під шкалою: колірний квадрат + назва сегмента + час, без відсотків самотужки (FR-P.6)
- Список сьогоднішніх записів під легендою — іконка категорії, назва, тривалість

**Віджет 4×2:**
- Та сама тришарова шкала, але заввишки ~8px — суттєво тихіша за кнопки під нею
- П'ять круглих кнопок категорій (FR-2.1) в один ряд, іконки Tabler-стилю, компактні підписи (напр. "Люди" замість повного "Живе спілкування" — обмежений простір)

**Картка тижневої рефлексії:** питання + три кнопки-діапазони (FR-P.1) + видимий підпис "Можна пропустити"

**Ключове дизайн-рішення, яке варто зберегти буквально при реалізації:** засічка орієнтиру — це просто тонка вертикальна лінія на нейтральному фоні, без жодної реакції на перевищення. Якщо з'явиться спокуса підсвітити перевищення кольором — це сигнал звірити рішення з розділом 4, не з інтуїцією "так зрозуміліше".


### 4.5 Дизайн-система (v3.1)

Джерело — Figma "App concept" (`k6s4prQ9oK9x2uUvzHRghR`) та "Everyday_Designs". Значення беруться **через MCP** (`get_design_context`/`get_metadata`/`download_assets`), а не зі скриншотів.

- **Тема лише світла** (градієнтний фон, `enableEdgeToEdge()`); шрифт Golos Text (заголовки Lora/Satoshi з ранніх фреймів замінені); лого-екран дозволів — Indie Flower
- **Кнопки — лише `TeperaButton`** (Primary/Secondary/Tertiary × Small/Medium/Big) і `TeperaIconButton`; ad-hoc Material-кнопок нема. Малі "ⓘ"/"×" у заголовках карток лишаються `IconButton`
- **`TeperaCard`**: скляний білий 80%, радіус 16, відступ 16, проміжок 12, без тіней, заголовок M3 titleMedium (16/24sp Medium). **`TeperaChip`**: тональний #DCF6ED, повне заокруглення, висота від 32dp, підпис 14sp Medium #006944 + значення SemiBold #003926 (AA-контраст)
- **Перемикач (Switch)**: увімкнений — #005E3E; вимкнений за M3 — суцільний світлий трек + рамка й ручка брендового #006944 (раніше напівпрозорий білий на "склі" майже зникав)
- Контекстні картки Home (Figma), чіпи запису й затримки воріт (11sp/14sp за окремими макетами) — власні виконання, не `TeperaChip`

## 5. Модель даних

```kotlin
enum class EntrySource { MANUAL, GAP_LABELED, HEALTH_CONNECT, SLEEP_API }
// GAP_LABELED (v2.6) — запис створено позначенням виявленої паузи (FR-D.5)
// HEALTH_CONNECT/SLEEP_API лишені в enum для сумісності з post-MVP, у MVP не використовуються

@Entity(tableName = "categories")
data class CategoryEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val name: String,
    val iconName: String,
    val colorHex: String,
    val isDefault: Boolean = false,
    val isCustom: Boolean = false, // MVP дозволяє до двох isCustom = true (FR-2.2, оновлено у v3.1)
    val isAutoTracked: Boolean = false, // залишено для post-MVP, у MVP завжди false
    val isHidden: Boolean = false,
    val sortOrder: Int = 0
)

@Entity(
    tableName = "activity_entries",
    foreignKeys = [ForeignKey(
        entity = CategoryEntity::class,
        parentColumns = ["id"],
        childColumns = ["categoryId"],
        onDelete = ForeignKey.CASCADE // db-страховка, з UI не викликається (FR-2.3)
    )],
    indices = [Index(value = ["categoryId"]), Index(value = ["startTime"])]
)
data class ActivityEntryEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val categoryId: String,
    val startTime: Long, // epoch ms, локальний час
    val durationMinutes: Int,
    val note: String? = null,
    val source: EntrySource = EntrySource.MANUAL,
    val createdAt: Long = System.currentTimeMillis()
)

// Нове в v2.0 — Exclusion List (FR-3.5)
@Entity(tableName = "excluded_apps")
data class ExcludedAppEntity(
    @PrimaryKey val packageName: String,
    val cachedLabel: String, // назва застосунку на момент додавання, кешується для UI
    val addedAt: Long = System.currentTimeMillis()
)

// Нове у v2.6 — виявлені паузи (FR-D.1, FR-D.5)
@Entity(tableName = "detected_gaps")
data class DetectedGapEntity(
    @PrimaryKey val id: String = UUID.randomUUID().toString(),
    val startTime: Long,        // epoch ms, локальний час
    val durationMinutes: Int,   // >= 30 (FR-D.1)
    val labeledEntryId: String? = null, // якщо позначено — id створеного ActivityEntry
    val dismissed: Boolean = false      // користувач свідомо пропустив; не питати повторно
)
// Непозначені й непропущені паузи лишаються доступними в історії безстроково,
// але застосунок ніколи про них не нагадує (FR-D.5)

// Нове у v3.1 (версія БД 9; міграції 2→3 … 8→9 явні, fallbackToDestructiveMigration не використовується)
@Entity(tableName = "sleep_windows") // T-12, FR-3.2: до 2 вікон
data class SleepWindowEntity(val slot: Int, val startMinuteOfDay: Int, val endMinuteOfDay: Int, val enabled: Boolean)
// endMinuteOfDay <= startMinuteOfDay означає перетин півночі

@Entity(tableName = "user_estimates") // T-3/T-10/T-14: власні оцінки користувача до показу реальності
data class UserEstimateEntity(
    @PrimaryKey val id: String, val type: EstimateType, // ONLINE_HOURS | LAST_PHONE_USE | UNLOCK_COUNT
    val estimatedValue: Long, val actualValue: Long?, // actualValue == null — реальність ще не розв'язана
    val forDate: LocalDate, val createdAt: Long // LocalDate зберігається як epoch-day (Converters)
)

@Entity(tableName = "app_gates") // FR-G.1–G.3
data class AppGateEntity(
    @PrimaryKey val packageName: String, val delaySeconds: Int, // 3 | 5 | 10 (старі 20 допускаються до першого тапу)
    val originalIconHandled: Boolean, val createdAt: Long,
    val lastProceedAtMillis: Long, // дебаунс 30 с (FR-G.3)
    val shortcutId: String // унікальний на кожне створення (FR-G.1)
)

@Entity(tableName = "gate_events") // FR-G.4: PROCEEDED | CANCELLED, по одній на рішення
data class GateEventEntity(@PrimaryKey val id: String, val packageName: String, val result: String, val atMillis: Long)

@Entity(tableName = "card_show_history") // FR-D.15: читається ТІЛЬКИ рушієм карток для частоти
data class CardShowEntity(@PrimaryKey val id: String, val cardType: String, val isEstimate: Boolean, val atMillis: Long, val result: String)

// DataStore (не Room): settings (орієнтир, прапорці онбордингу, пресет чутливості FR-D.12, вибір кнопок віджета FR-4.8,
// ключі закриття карток), active_timers (ефемерні таймери FR-1.6), sub_minute (секунди <1 хв, ключ "yyyyMMdd|categoryId", FR-1.7).
// Лічильники sub_minute не входять в експорт/імпорт JSON (FR-6.2).

// Налаштування (орієнтир Online-часу, вікно сну) — зберігати через DataStore Preferences,
// не окрему Room-таблицю (одне значення, немає сенсу в реляційній структурі)
```

---

## 6. Нефункціональні вимоги

### 5.1 Продуктивність
Холодний старт до 2с. Читання `UsageStatsManager` — потенційно повільніша операція за прямі Room-запити; виконувати на `Dispatchers.IO`, кешувати результат на час сесії, не запитувати на кожен recomposition.

### 5.2 Безпека та приватність
- Аналітика: лише crash-репортинг (Firebase Crashlytics), без usage-аналітики, без рекламних SDK
- **Оновлено в v2.0:** застосунок читає статистику використання ВСІХ застосунків на пристрої через `UsageStatsManager` — найчутливіші дані в цьому MVP. Дані обробляються й лишаються **виключно локально**, ніколи не передаються на сервер, не логуються в crash-репорти. Це прямо й чесно вказується в Privacy Policy та Data Safety form (FR-7.3)
- Шифрування даних на диску — поза обсягом MVP

### 5.3 Надійність
Явна стратегія Room-міграцій з першої версії БД, без `fallbackToDestructiveMigration()` — **підтверджено на практиці:** зміна схеми без bump версії й `Migration` падає з `IllegalStateException` при першому запуску на пристрої зі старою схемою (відтворено на Samsung S23 при `adb install -r`). Bump версії й Migration — щоразу, без винятків. Поточна версія БД — 9.

### 5.4 Юзабіліті та доступність
Базовий рівень — системні дефолти Android/Compose semantics, без додаткової доробки на MVP.

### 5.5 Локалізація
Українська та англійська з першого релізу, усі рядки в string resources; перемикач мови застосунку — FR-6.7 (Системна/Українська/English).

### 5.6 Сумісність
- `minSdkVersion` = 26, `targetSdkVersion` = 36 (обов'язково з 31.08.2026)
- Тестова матриця: Samsung S23 (Android 16) — основний пристрій перевірки; Motorola G84 5G (Android 15) — перевірено раніше на старому стилі віджета; Huawei P9 (Android 8, EMUI) — перевірено ворота й екран паузи; Sony XZ1 Compact (Android 9) — свідомо відкладено. **Новий стиль віджетів, Статистика "Вчора", Щоденник 14 діб і секунди <1 хв перевірені лише на S23**
- `UsageStatsManager` доступний з Android 5.0+, сумісний з мінімальною версією без обмежень (на відміну від Health Connect, який мав нижню межу Android 9)

### 5.7 Архітектурна готовність до розвитку
| ID | Вимога |
|---|---|
| NFR-7.1 | UUID замість autoincrement Long як первинний ключ |
| NFR-7.2 | Repository-абстракція між UI/ViewModel і Room |
| NFR-7.3 | Локальний анонімний device-ID при першому запуску |
| NFR-7.4 | Поле для медіа (якщо з'явиться) — URI, не blob |

---

## 7. Явно поза обсягом MVP

- Автоматичний трекінг через Health Connect / Sleep API (**перенесено з v1.5 — тепер офіційно post-MVP**, розділ 8)
- Live-оновлюваний таймер на віджеті в реальному часі (FR-4.2 — свідома спрощена версія)
- Необмежена кількість користувацьких категорій (обмежено двома, FR-2.2)
- Формальна KMP-структура проєкту (дані проєктовані платформо-нейтрально, але сама multiplatform-інфраструктура — не зараз, розділ 2.6)
- DI-фреймворк (Hilt/Koin) — ручний factory pattern
- Мультимодульна Gradle-структура
- CI/CD-автоматизація
- Повне автоматизоване тестове покриття — лише мінімальні smoke-тести
- Шифрування даних на диску
- Складна кастомна анімація
- Мультипрофілі/сімейний режим
- Хмарний бекенд і синхронізація
- Геозони/локаційна автоматизація
- Справжній соціальний шар (стрічка/друзі)
- Власний background-сервіс для трекінгу (лише системні API)
- Власний chart-движок "з нуля" (використовується Vico)
- AccessibilityService-воріт і перехоплення відкриття застосунків через сповіщення (FR-G.5) — свідомо не робимо навіть post-MVP без нового явного запиту
- Платні функції, що щось блокують, реклама, прямий Play Billing (платіжний шар лише RevenueCat, розділ 3.11; Pro нічого не гейтить)
- Топ застосунків за Online у Статистиці (свідомо: надто близько до "оцінки")

---

## 8. Орієнтовний графік (уточнено в v2.0)

| Фаза | Тривалість | Зміст |
|---|---|---|
| 0. Налаштування та архітектура | 2 тижні | Проєкт, Room + Repository, UUID-схема, Theme.kt за вже встановленим стилем; паралельно — Google Play Developer акаунт, Play App Signing |
| 1. Базові екрани | 4 тижні | Category CRUD (5 категорій + 1 користувацька), Home, Додавання запису, перевірка перекриття, онбординг-питання про цінності (FR-P.2) |
| 2. Структура доби | 4 тижні | **Спершу спайк:** перевірити на реальному пристрої, чи `queryUsageStats()` повертає повний список застосунків без `QUERY_ALL_PACKAGES`, чи package visibility filtering (Android 11+) все одно блокує Exclusion List — це визначає, чи потрібен другий чутливий дозвіл (розділ 10, ризики). Далі: інтеграція `queryEvents()` (не `queryUsageStats()` — потрібен потік подій для детекції пауз, FR-D), Grace Period Buffer формула, час останнього використання (FR-D.7), Exclusion List, fallback без дозволу |
| 3. Віджет | 3 тижні | Jetpack Glance 4x1/4x2, WorkManager-оновлення ~30 хв, контекстне сортування, "5-й слот", Quick Settings tile |
| 4. Статистика, картки та патерни | 4 тижні | Vico-графіки (День/Тиждень/Місяць), тренд сегментів доби, механізм "оцінка → реальність" (FR-P.1), детекція пауз і картка позначення (FR-D.1–D.5), стек контекстних карток (FR-D.10–D.11), тепловий патерн доби (FR-D.8–D.9) |
| 5. Локалізація, доступність, бекап | 2 тижні | UA/EN, Auto Backup + експорт/імпорт, Crashlytics |
| 6. QA та підготовка до релізу | 3 тижні | Тестування на реальних пристроях, Privacy Policy, Data Safety form (з явним розкриттям Usage Access), Content Rating, store listing |
| 7. Закрите тестування Google Play | 2+ тижні | Активний рекрутинг 12+ тестувальників + 14 днів опт-ін |
| 8. Реліз | 1 тиждень | Подача на публічний реліз |

**Орієнтовний підсумок: ~25-27 тижнів (близько 6 місяців).** Зростання відносно v2.5 (+3 тижні) — через перехід на `queryEvents()`, детекцію пауз, систему карток і тепловий патерн (розділ 3.8). Трохи довше за v1.5 (18-20 тижнів) — приблизний виграш від виключення Health Connect/Sleep API компенсується складнішим Jetpack Glance-віджетом і новою UsageStatsManager-логікою. Буфер +20% на непередбачені складнощі лишається чинним.

---

## 9. Критерії успіху MVP

**Важливе застереження щодо очікувань:** доказова база не дає підстав очікувати значного падіння екранного часу. РКД активних nudge-втручань (n=17, тобто слабкий доказ) не виявило значущого ефекту на сам показник екранного часу. Застосунки цієї категорії дають скромний і часто короткочасний ефект на метрику годин. Побудова очікувань навколо "користувачі зменшать скролінг на 30%" веде до спокуси додати стріки й тиск — що зруйнує саму основу продукту (розділ 4).

### 9.1 Реліз
Успішний публічний реліз у Google Play + перші реальні користувачі (органічні, через Threads-аудиторію).

### 9.2 Продуктова здоровість (перші місяці)
- Люди повертаються логувати через 2–4 тижні (утримання важливіше за встановлення)
- Дозвіл на статистику використання надають — якщо більшість зупиняється на цьому екрані, проблема в поясненні (FR-3.11), а не в продукті
- Люди користуються Exclusion List — ознака, що число почало сприйматись як своє, а не як звинувачення

### 9.3 Головний критерій — якісний, не вимірюваний аналітикою
Оскільки в MVP свідомо немає usage-аналітики (лише crash-репорти, NFR 6.2), основне джерело сигналу — відгуки в Play Store і Threads-аудиторія.

- **Успіх звучить як:** "вперше застосунок про екранний час не змусив мене почуватись погано"
- **Провал звучить як:** "цифри лякають", "відчуваю провину" — **навіть якщо утримання при цьому високе**

### 9.4 Реалістичні очікувані ефекти (у порядку ймовірності)
1. Людина вперше бачить чесне число без внутрішнього спротиву (завдяки Exclusion List)
2. З'являється звичка помічати офлайн-моменти — сам акт логування цінніший за статистику
3. Зміщується не кількість часу, а його усвідомленість
4. У частини людей — поступове зменшення екранного часу, повільне й без драми

## 10. Пост-MVP (довідково)
Health Connect / Sleep API автоматичний трекінг, live-таймер на віджеті, необмежена кастомізація категорій, детальна аналітика й тренди за місяці/роки, розумні інсайти й контекстні підказки, справжній соціальний шар, хмарна синхронізація, iOS-реліз, стріки/цілі, фото до записів, push-нагадування, експорт CSV/PDF, Wear OS, монетизація, **"ворітна" іконка для обраних застосунків (розділ 10.2)**.

### 10.1 iOS-реліз — технічна примітка на майбутнє
Перенесення на iOS реалістичне через **Kotlin Multiplatform (KMP)** — Room (стабільно з 2.8.2), DataStore і Lifecycle/ViewModel уже мають стабільну KMP-підтримку, і наша архітектура (UUID-ключі, Repository-абстракція, платформо-нейтральні data classes) вже готова до цього шляху без переробки.

**Переноситься в спільний `shared`-модуль майже без змін:** Entity-класи, Room-схема й DAO, бізнес-логіка Repository (перевірка перекриття, архівація категорій, формула Grace Period Buffer), DataStore-налаштування.

**Потребує повністю нативного написання, без спільного коду:** UI-шар (SwiftUI або Compose Multiplatform), Jetpack Glance-віджет → WidgetKit, Quick Settings tile (концепції немає на iOS), WorkManager → BGTaskScheduler.

**Критичне архітектурне обмеження — ядро продукту потребує переосмислення, не порту:** `UsageStatsManager` не має прямого еквівалента на iOS. Apple's Screen Time API (Family Controls / DeviceActivity) працює через ізольований App Extension (`DeviceActivityReport`) з токенізованими (privacy-preserving) ідентифікаторами застосунків, і передача підсумкових чисел з extension назад в основний застосунок — нетривіальна, не до кінця усталена в спільноті задача. **Сегмент Online у структурі доби на iOS доведеться проєктувати заново під ці обмеження, а не переносити як є.**

### 10.2 FR-G — "Ворітна" іконка (post-MVP кандидат)

**Статус (оновлено у v3.1):** ідея **реалізована в MVP** — див. розділ 3.9 (FR-G.1–G.6). Опис нижче лишено як історію рішення; актуальна поведінка — у 3.9 (затримка 3/5/10 с, екран паузи з дихальною анімацією, кнопки "Продовжити"/"Вийти").

**Призначення.** Дати користувачу самостійно налаштувати паузу перед відкриттям обраних ним застосунків — без перехоплення системного рівня, без AccessibilityService. Розглядалась і відхилена для MVP альтернатива — перехоплення запуску застосунку через AccessibilityService/overlay (як у конкурента One Sec) — з двох причин: (1) заводить продукт у перенасичену нішу блокувальників із домінуючим гравцем (peer-reviewed дослідження з Max Planck, >100k відгуків); (2) чотири чутливі дозволи одночасно (usage stats, можлива видимість пакетів, overlay, accessibility) різко підвищують ризик відхилення при Google Play review для нового акаунта розробника, і Android 17 Advanced Protection Mode автоматично відкликає AccessibilityService у застосунків, не класифікованих як інструменти доступності.

**User flow:**
1. Користувач у Tepera відкриває розділ "Застосунки з затримкою" (робоча назва)
2. Обирає застосунок(-ки) зі списку встановлених
3. Tepera створює закріплений ярлик (pin shortcut) на домашньому екрані, що замінює візуально оригінальну іконку
4. Користувач сам видаляє чи ховає оригінальну іконку (Tepera не може зробити це за нього — системне обмеження Android)
5. Тап по новому ярлику → екран-оверлей Tepera: повідомлення + прогрес-бар на N секунд (дефолт, наприклад, 5-10) + кнопка "Не зараз"
6. Якщо дочекався — інтент відкриває цільовий застосунок
7. Якщо "Не зараз" — повернення на домашній екран, дія фіксується як подія для власної статистики, без оцінки (розділ 4)

**Технічний підхід:**
- `ShortcutManager.requestPinShortcut()` — доступний з API 25, сумісний із minSdk 26
- `<queries>` у маніфесті з конкретними package name обраних застосунків — **не** `QUERY_ALL_PACKAGES`
- Compose-екран з таймером + `Intent` на цільовий пакет після завершення
- Жодного AccessibilityService, жодного overlay поверх сторонніх застосунків, жодних нових чутливих дозволів понад те, що вже є в MVP

**Явні обмеження (озвучити чесно в UI, не приховувати):**
- Обхід тривіальний — застосунок усе ще доступний через шухляду застосунків, "Нещодавні", пуш-сповіщення
- Це self-nudge інструмент для людини, яка сама хоче собі допомогти, а не блокування
- Оригінальна іконка застосунку лишається на місці, поки користувач сам її не сховає — технічне обмеження Android, не недоробка

**Психологічна відповідність (розділ 4):** автономія (користувач сам обирає застосунки й може відкотити); відсутність оцінки (подія "не зараз" не подається як перемога, "зачекав і зайшов" — не як поразка); на відміну від конкурента, чия задокументована ефективність побудована на дратівливості механізму, наш варіант свідомо без фрустрації як інструменту — залишається відкритим питанням для майбутнього A/B, чи це послаблює ефект.

**Перед реалізацією — обов'язкові кроки:**
1. Спайк: `requestPinShortcut` + запуск через `<queries>` на всіх 4 тестових пристроях, окремо Huawei/EMUI
2. Відповісти на питання "чим ми кращі за One Sec" — якщо відповіді нема, не робити
3. Перевірити реакцію ранніх користувачів MVP (відгуки, Threads) — чи взагалі просять цю функцію

## 11. Ризики
| Ризик | Вплив | Мітигація |
|---|---|---|
| Google Play review/rejection через чутливий дозвіл `PACKAGE_USAGE_STATS` | Високий | Чіткий онбординг-пояснення, детальна Data Safety form, готовність до додаткового review-циклу Google (типово для цього дозволу) |
| Exclusion List (FR-3.5) може потребувати ще й `QUERY_ALL_PACKAGES` через package visibility filtering (Android 11+) — другий чутливий дозвіл посилює ризик review | Середній-Високий | Спайк-тест на початку фази 2 (перевірити реальну поведінку `queryUsageStats()` щодо видимості пакетів), перш ніж проєктувати UI Exclusion List навколо неперевіреного припущення |
| Конкурентне позиціонування зміщується до перенасиченої категорії "screen time trackers" | Середній | Диференціація через категорійний офлайн-трекінг + Monastic-дизайн без блокування — задокументовано, не приховано |
| Користувачі не завершують ручний крок надання Usage Access (типова проблема цього дозволу — низький completion rate) | Середній | Чіткий заклик до дії з поясненням "навіщо" (FR-7.1), fallback — застосунок функціональний і без цього для офлайн-логування |
| Sleep API/Health Connect відсутні в MVP — менше "автоматичної магії" при першому знайомстві | Низький | Свідомий компроміс заради простішого MVP; повертається в post-MVP |
| Детекція пауз читається як стеження ("застосунок помітив, що я не брав телефон") | Середній | Формулювання карток — про час, не про спостереження за людиною; жодних push; ініціатива лише всередині застосунку (FR-D.3) |
| Стек карток перетворює спокійний головний екран на стрічку | Середній | Жорсткий ліміт 3 картки, вертикальний стек замість каруселі, компактна форма (FR-D.10–D.11); при сумніві — прибирати картки, не додавати |
| `queryEvents()` поводиться інакше на різних OEM (особливо EMUI) | Середній-Високий | Розширений спайк у фазі 2 на всіх 4 пристроях; детекція пауз — не критичний шлях, застосунок лишається робочим без неї |
| Втрата upload-ключа підписання застосунку | Високий | Play App Signing налаштовується в фазі 0 |
| Рекрутинг 12 тестувальників на 14 днів | Середній | Threads-аудиторія (~400 підписників, 150-300k переглядів/міс), почати заздалегідь |

| Віджети поводяться по-різному на лаунчерах: Glance не повідомляє реальну висоту (`LocalSize` — найближчий менший розмір зі списку), RemoteViews губить зайві діти контейнера, 1x1 на Samsung — лише 76dp, `cornerRadius` працює з API 31 | Середній | Сітка доби малюється однією Bitmap, кругле тло — drawable-ами, 1x1/2x1 на `SizeMode.Exact` зі стисненням; перевірка на всіх 4 пристроях перед релізом (новий стиль — лише S23) |
| Платіжний шар (RevenueCat) без перевірки реальної оплати в Google Play; розбіжність Data Safety і фактичного маніфесту (транзитивні `INTERNET`, `WAKE_LOCK`, `FOREGROUND_SERVICE`, `RECEIVE_BOOT_COMPLETED` від Crashlytics/WorkManager) | Середній | Перевірити release-збірку з `goog_` ключем у закритому тестуванні; Data Safety заповнювати за фінальним змердженим маніфестом (`docs/play-console-declarations.md`) |
| Розбіжність метрик між екранами: Home "Офлайн-життя" ще на старій формулі, Статистика — за об'єднанням (FR-3.13) | Низький | Вирівняти Home тим самим підходом (розділ 12) |

## 12. Відкриті питання
- ~~Точний набір 5 дефолтних категорій~~ — **підтверджено, оновлено у v2.4:** Природа, Читання, Хобі/Творчість, Рух/Спорт, Живе спілкування (Сон прибрано, розділ 3.3)
- **Інтервал періодичного оновлення віджета** — робочий дефолт 30 хв, не заперечено, приймається як робоче рішення до першого практичного тестування (можна скоригувати після спостереження за впливом на батарею)
- **Package visibility для Exclusion List** — див. спайк-задачу фази 2 і ризик у розділі 10
- **FR-3.3 (timezone)** — чи прийнятне обмеження "лише локальний час" з огляду на подорожі
- ~~Баланс віджета після зміни ролі ручного логування (v2.6)~~ — **вирішено:** T-7 віддав більше ваги структурі дня, а потім віджет повністю перероблено за Figma (FR-4.1, 4.9): ряд кнопок у капсулі + картка сітки доби у 4x2
- ~~Іконка застосунку — фінальна графіка~~ — **вирішено у v3.1:** Figma node 234:435 (небо, два пагорби, логотип "Т"), adaptive-іконка + 512×512 PNG для Google Play (`docs/play-store/icon-512.png`)
- **Вирівняти Home "Офлайн-життя" з FR-3.13** (об'єднання Online й записів, без подвійного віднімання) — чи змінювати Home тією самою формулою, щоб числа на двох екранах були сумісні
- **URL Google Forms для FR-6.4** (`FeedbackForm.UA_FORM_URL`/`EN_FORM_URL`) — досі плейсхолдери, стейкхолдер має створити форми перед релізом
- **Sony XZ1 Compact і нова розкладка віджетів на Huawei P9/Motorola G84** — не перевірялись; потрібні перед закритим тестуванням
- **T-15 (спайк імпульсивних мікровзаємодій) і T-9** — заплановані, кожен окремою сесією; T-16 частина Б заблокована рішенням власника
- **Чи потрібна "Місячна" статистика** — у v3.1 період "Місяць" прибрано (FR-5.2); повернути, якщо буде запит

## 13. Прогалини покриття (додано у v3.0)

Виявлено при побудові карти екранів і звірці сценаріїв. Кожна позиція — те, що вимоги обіцяють або мають на увазі, але для чого немає ані екрана, ані описаної поведінки. Впорядковано за серйозністю.

### 13.1 Критичні

| ID | Прогалина | Суть | Куди вбудувати |
|---|---|---|---|
| GAP-1 | **Ніщо не вчить додати віджет** | Віджет — головна щоденна взаємодія за Trampoline UX, але в онбордингу (F-1) про нього жодного слова. Людина проходить три екрани, потрапляє на Головну й може ніколи не дізнатися, що основний сценарій живе поза застосунком. Це не відсутній екран, а відсутній **крок онбордингу** | Додати крок після надання дозволу: пояснення + `requestPinAppWidget()` (Android 8+) для додавання в один тап. Пропуск завжди доступний |
| GAP-2 | **Дозвіл, відкликаний пізніше** | Людина надала доступ, через місяць прибрала його в системних налаштуваннях. Поведінка не описана ніде — найімовірніший результат у коді це порожні графіки без пояснення | Перевірка доступу при кожному старті; тиха деградація до режиму FR-3.12; картка дозволу повертається в чергу |
| GAP-3 | **Екран редагування запису відсутній** | FR-1.4 обіцяє "редагування будь-якого поля", але екрана немає ні в одному флоу. Особливо потрібен для записів після автозупинки таймера (GAP-4) | Новий екран: категорія, час початку, тривалість, нотатка, видалення |

### 13.2 Важливі

| ID | Прогалина | Суть | Куди вбудувати |
|---|---|---|---|
| GAP-4 | **Запис "тривалість під питанням" нікуди не веде** | FR-4.7 створює такий запис після автозупинки через 4 год і каже, що його можна виправити в історії — але способу знайти саме такі записи немає | Візуальна позначка в списку записів + перехід на екран редагування (GAP-3) |
| GAP-5 | **Стан активного таймера в застосунку** | Таймер, запущений з віджета, ніде не видно при відкритті застосунку. Немає способу зупинити його зсередини | Постійний індикатор на Головній, коли таймер працює |
| GAP-6 | **Quick Settings tile не має вибору категорії** | Плитка логує "швидке додавання" — але якої категорії? Не визначено | Рішення: прив'язка до однієї категорії в налаштуваннях **або** відкриття діалогу вибору. Потребує рішення стейкхолдера |
| GAP-7 | **Налаштування віджета** | Які 5 категорій показує віджет, якщо користувач приховав дефолтну або додав власну (FR-2.2, FR-2.3)? Не визначено | Екран конфігурації віджета або автоматичний вибір за `sortOrder` серед неприхованих |
| GAP-8 | **Екран "Про застосунок"** | Посилання на Privacy Policy має бути доступне **з самого застосунку** — це вимога Google Play, не косметика. Також версія застосунку й контакт | Найдешевший з усіх пунктів. Додати в "Ще" |

### 13.3 Дрібні

| ID | Прогалина | Суть |
|---|---|---|
| GAP-9 | **Порожні стани першого дня** | Систематично описаний лише порожній стан патерну (FR-D.9). Головна й Статистика в день встановлення — ні. За розділом 4: без докору, "Поки порожньо", не "Ти нічого не зафіксував" |
| GAP-10 | **Помилки імпорту** | Зіпсований або чужий JSON (FR-6.2) — поведінка не описана. Мінімум: не втратити наявні дані, показати спокійне повідомлення |
| GAP-11 | **Мова й тема** | Не визначено, чи йдуть за системою, чи налаштовуються вручну. Рекомендація для MVP: за системою, без окремих налаштувань |

### 13.4 Впорядкування робіт

GAP-8 і GAP-9 — найдешевші, робити разом із відповідними фазами. GAP-1 і GAP-2 — у фазу 2 (онбординг і дозволи). GAP-3, GAP-4, GAP-5 пов'язані між собою й робляться одним блоком у фазі 1. GAP-6 і GAP-7 — у фазу 3, разом із віджетом.

### 13.5 Статус на v3.1

| ID | Статус | Примітка |
|---|---|---|
| GAP-1 | **Закрито** | Крок "Пропозиція віджета" в онбордингу (FR-4.10), перевірено на пристрої |
| GAP-2 | Відкрито / не перевірено окремо | Доступ перевіряється при кожному поверненні на Home, без доступу — тиха деградація до FR-3.12; окремо сценарій "відкликав через місяць" наживо не перевіряли |
| GAP-3 | **Закрито** | Екран редагування запису (FR-1.4) |
| GAP-4 | **Закрито зміною рішення** | Автозупинки й записів "тривалість під питанням" більше нема (FR-4.7) |
| GAP-5 | Частково | Активна категорія позначена рамкою на картці Home й вибраним станом кнопки віджета; окремого постійного індикатора нема |
| GAP-6 | **Закрито** | Плитка відкриває екран додавання запису (FR-4.4) |
| GAP-7 | **Закрито** | Налаштування кнопок віджета (FR-4.8) |
| GAP-8 | Відкрито | Екрана "Про застосунок" (посилання на Privacy Policy, версія, контакт) ще нема — вимога Google Play |
| GAP-9 | Частково | Порожній стан Home — "Поки порожньо"; Статистика "Вчора" без даних — порожня; окремий аудит порожніх станів першого дня не проводився |
| GAP-10 | **Закрито** | Спокійне повідомлення й збереження даних при помилці (FR-6.2) |
| GAP-11 | **Закрито** | Мова — перемикач Системна/UA/EN, тема — завжди світла (FR-6.7) |

## 14. Вимоги до публікації в Google Play (Publishing Checklist)

### 14.1 Передумови
| ID | Вимога | Статус |
|---|---|---|
| PUB-1 | Google Play Developer акаунт (особистий, $25, верифікація особи) | **Створено й активовано** (станом на v3.1) |
| PUB-2 | Privacy Policy на GitHub Pages | Рішення прийнято, реалізація — фаза 7 |
| PUB-3 | `applicationId` | Вирішено: `com.serkodesign.tepera` |

### 14.2 Технічні вимоги збірки
| ID | Вимога |
|---|---|
| PUB-4 | `targetSdkVersion` = 36 (Android 16) — обов'язково з 31.08.2026 |
| PUB-5 | Формат — Android App Bundle (.aab), не APK |
| PUB-6 | Play App Signing — налаштувати в фазі 0, не відкладати (втрата upload-ключа блокує майбутні оновлення назавжди) |
| PUB-7 | 16 KB memory page size alignment — низький ризик для чистого Kotlin/Compose без NDK, перевірити при фінальній збірці |

### 14.3 Store listing та комплаєнс-форми
| ID | Вимога |
|---|---|
| PUB-8 | Content Rating Questionnaire (IARC) |
| PUB-9 | Target audience & content — явно 20-45, не для дітей (Families self-certification) |
| PUB-10 | Data Safety form — **оновлено в v2.0:** тепер має явно розкривати доступ до `PACKAGE_USAGE_STATS` (статистика використання застосунків), окрім Crashlytics-даних. Це чутливий дозвіл — Google Play може вимагати додаткового обґрунтування призначення через Permissions Declaration Form при review |
| PUB-11 | Store listing assets: іконка 512×512, feature graphic, скріншоти, описи UA/EN | Іконка 512×512 готова (`docs/play-store/icon-512.png`, з Figma); описи UA/EN — `docs/store-listing-ua.md`/`-en.md`; feature graphic і скріншоти — у репозиторії не відстежуються (статус невідомий) |
| PUB-12 | Контактний email підтримки |
| PUB-13 | Категорія застосунку (Health & Fitness чи Lifestyle) |

### 14.4 Не застосовується
- Форма видалення акаунта/даних — немає акаунтів
- ~~Вимоги до платіжних систем~~ — **тепер застосовується (v3.1):** RevenueCat, добровільна підтримка й прихований Pro (розділ 3.11); для Data Safety/Payments-декларації див. `docs/revenuecat-setup.md` і `docs/play-console-declarations.md`
